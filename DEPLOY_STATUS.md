# 🚀 ডিপ্লয় স্ট্যাটাস — ১০ সেপ্টেম্বর ২০২৬

**লাইভ URL:** https://edusob.pages.dev
**সর্বশেষ ডিপ্লয়:** `e8cd374d` (production, branch `main`)
**GitHub `main`:** `e181829`
**D1 ব্যাকআপ:** `data/edusob-production-2026-09-10.sql` (৩৯৬ KB — গিট-ইগনোর করা, রিপোতে নেই)

---

## ১. কী কী পুশ ও ডিপ্লয় হলো

| কমিট | কী |
|---|---|
| `6d98a50` | অডিট-ভিত্তিক P0 ফিক্স: XSS, সেশন-ডিলিট বাগ, `'\n'` ক্র্যাশ, /results স্পিড, ডিজাইন সিস্টেম v2, মোবাইল ওভারফ্লো |
| `920b107` | `DEPLOY.md` রানবুক |
| `be49246` | ডেটাবেস/কুকিজ গিট-ট্র্যাকিং বন্ধ, অ্যাডমিন পাসওয়ার্ড-রিসেট বাগ ফিক্স, রিপোর্ট থেকে ক্রেডেনশিয়াল রিড্যাক্ট |
| `a121eb6` | সিকিউরিটি হেডার (`_headers`), স্ট্যাটিক ক্যাশিং, ডিপ্লয়-ওয়ার্কফ্লো টেমপ্লেট |
| `11b2aad` | SSR পেইজে সিকিউরিটি হেডার (ওয়ার্কার মিডলওয়্যার) |

> ⚠️ **পুশ ঠিকঠাক হয়েছে, কিন্তু Cloudflare স্বয়ংক্রিয়ভাবে বিল্ড করেনি।**
> কারণ: Pages-এর git-connected GitHub App ট্রিগার বন্ধ — `main`-এ পুশ যাচ্ছে (GitHub-এ `11b2aad` আছে),
> কিন্তু ২০২৬-০৯-০৯-এর পর আর একটাও `github:push` ডিপ্লয় তৈরি হয়নি।
> তাই আমি `wrangler pages deploy` দিয়ে **সরাসরি ডিপ্লয়** করেছি — প্রোডাকশন এখন সর্বশেষ কোডেই আছে।

---

## ২. প্রোডাকশনে যা যাচাই করা হয়েছে ✅

**রাউটসমূহ (সব HTTP 200)** — `/`, `/dashboard`, `/admin`, `/results`, `/qpapers`, `/syllabus`, `/wallet`, `/profile`, `/news`, `/jobs`, `/notices`, `/cv`, `/scholarships`

**ফিচার**

- `/qpapers` ও `/syllabus` — আগে `'\n'` ক্র্যাশে ৫০০ দিত, এখন ২০০ ✅
- `/api/admin/stats` → `stats.trends` (today/yesterday/delta) লাইভ ✅
- `/api/link-status/batch` → ক্যাশ কাজ করছে: ১ম কল `cached=0 checked=5`, ২য় কল `cached=5 checked=0` ✅
- D1 সংযুক্ত ও সাড়া দিচ্ছে (admin লগইন → `role: admin`) ✅

**কর্মদক্ষতা (প্রোডাকশন, ৫টি URL)**

| অবস্থা | সময় |
|---|---|
| কোল্ড (৫টি রিয়াল প্রোব) | ৩.৯৯ সে. |
| ওয়ার্ম (ক্যাশ থেকে) | **০.৩১–০.৬০ সে.** |

**নিরাপত্তা হেডার (লাইভ, সব SSR পেইজে)**

```
x-content-type-options: nosniff
referrer-policy: strict-origin-when-cross-origin
x-frame-options: SAMEORIGIN
permissions-policy: camera=(), microphone=(self), geolocation=(), interest-cohort=()
```

**কোনো CDN নির্ভরতা নেই** — `fonts.googleapis.com: 0`, `jsdelivr: 0`। ফন্ট ও আইকন সব সেলফ-হোস্টেড।

---

## ৩. পুশের আগে যা ঠিক করতে হয়েছিল (রিপো **পাবলিক**)

1. **`data/*.sqlite` ও `cookies.txt` গিটে ট্র্যাকড ছিল** — অর্থাৎ যে কেউ রিপো ক্লোন করলে লাইভ ইউজার-ডেটাবেস পেত।
   এখন `.gitignore`-এ আছে এবং ইনডেক্স থেকে সরানো হয়েছে (ফাইল ডিস্কে আছে, লোকাল ডেভ কাজ করবে)।
2. **অ্যাডমিন পাসওয়ার্ড-রিসেট বাগ** — `initDatabase()`-এর `else` শাখা প্রতিবার বুটে `WHERE role='admin'` দিয়ে সব অ্যাডমিনের পাসওয়ার্ড ওভাররাইট করতো। এখন শুধু নতুন ডেটাবেসে সিড হয়, বিদ্যমান অ্যাডমিনের পাসওয়ার্ড আর ছোঁয় না।
3. **`AUDIT_REPORT.md`-এ প্লেইন-টেক্সট পাসওয়ার্ড** — আমি নিজে লিখেছিলাম, রিড্যাক্ট করা হয়েছে।

---

## ৪. 🔴 আপনাকেই করতে হবে (আমি করতে পারি না)

1. **টোকেন দুটি রিভোক করুন** — কাজ শেষ।
   - GitHub → https://github.com/settings/tokens
   - Cloudflare → https://dash.cloudflare.com/profile/api-tokens
2. **অ্যাডমিন পাসওয়ার্ড বদলুন** (`Ab52944820@`) — এটি গিট হিস্ট্রিতে পাবলিক আছে।
3. **গিট হিস্ট্রি পরিষ্কার করুন** (না হলে `data/` হিস্ট্রিতে থাকবেই):
   ```bash
   git filter-repo --path data/ --path cookies.txt --invert-paths --force
   git push --force
   ```
4. **স্বয়ংক্রিয় ডিপ্লয় চালু করুন** — দুটির যেকোনো একটি:
   - **(ক) সহজ:** Cloudflare Dashboard → Workers & Pages → `edusob` → git সংযোগ আবার লিঙ্ক করুন।
   - **(খ) নির্ভরযোগ্য:** `docs/deploy-workflow.example.yml`-কে `.github/workflows/deploy.yml`-এ কপি করুন,
     আর GitHub → Settings → Secrets-এ `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` যোগ করুন।
     ⚠️ `.github/workflows/**` পুশ করতে PAT-এ **`workflow` স্কোপ** লাগবে — তাই ফাইলটি আপাতত `docs/`-এ টেমপ্লেট হিসেবে রাখা হয়েছে।

---

## ৫. দ্বিতীয় রাউন্ড — stored-XSS বন্ধ + H1 (সম্পন্ন)

**এডমিন প্যানেলে stored-XSS ছিল — প্রমাণিত, ঠিক করা, নেগেটিভ-কন্ট্রোল দিয়ে যাচাই।**

অ্যাডমিন প্যানেল `onclick="openRoleModalById(1, '<নাম>', ...)"` আকারে বাটন বানাতো এবং
ভেতরের মান `esc()` দিয়ে এস্কেপ করতো। কিন্তু `esc()` শুধু `& < > "` এস্কেপ করে — **(`') নয়**।
ব্রাউজার আগে অ্যাট্রিবিউটটি HTML-ডিফোড করে, এরপর JS পার্স করে, তাই একটি সাধারণ `'` দিয়েই
স্ট্রিং ভেঙে কোড ইনজেক্ট করা যায়। সাইনআপ `name_bn`-এ শুধু `<>` ফিল্টার করে, `'` নয়।

**প্রমাণ (রিয়েল ব্রাউজার, Chromium):**

```
সাধারণ সাইনআপ পথেই:  name_bn = "রহিম'); alert('XSS-PWNED'); //"
এডমিন Users ট্যাবে "রোল" বাটনে ক্লিক →
   আগে  : alert('XSS-PWNED') ফায়ার  ❌  (dialogs: ["XSS-PWNED","XSS-PWNED"])
   এখন  : ২৪টি বাটন ক্লিক, ০ alert  ✅  (onclick: 'রহিম\'); alert(\'XSS-PWNED\'); //')
```

**ফিক্স**

| হেল্পার | কাজ |
|---|---|
| `jsq()` | ব্যাকস্ল্যাশ ও `'` কে JS-এস্কেপ → এরপর HTML-এস্কেপ। ৬টি `onclick` কল-সাইটে `esc()` বদলে ব্যবহার |
| `safeUrl()` | `href`-এ `javascript:` / `data:` স্কিম বন্ধ — `esc()` দিয়ে আটকানো যায় না |
| `esc()` | `toastMsg` ও `logBox`-এর আনএস্কেপড ইন্টারপোলেশনে প্রয়োগ |

> নোট: `jsq()`-এ `String.fromCharCode(92)` ব্যবহার করা হয়েছে — সোর্সে কোনো ব্যাকস্ল্যাশ
> লিটারেল নেই। অ্যাডমিনের JS একটি সিঙ্গেল-কোটেড স্ট্রিং-এর ভেতরে এমবেড করা, সেখানে `\n`
> লিখলে আসল নিউলাইনে পরিণত হয়ে রেজেক্স ভেঙে যায় (প্রথম চেষ্টায় সত্যিই ভেঙেছিল)।

**H1 (a11y/SEO):** `/wallet` ও `/admin/shop`-এ কোনো H1 ছিলই না — যোগ করা হয়েছে।
যাচাই: `h1audit.mjs` → **৩২টি রুটেই ঠিক একটি H1** (আগে ২টি রুটে ০, ১টিতে ভুল-পজিটিভ)।

**রিগ্রেশন যাচাই:** `tsc` clean · ১১১ ইনলাইন স্ক্রিপ্ট ব্লক, ০ broken ·
২৪ রুট × ২ ভিউপোর্ট → **০ সমস্যা** (কোনো ওভারফ্লো বা কনসোল-এরর নেই)।

**নতুন টেস্ট টুলিং:** `h1audit.mjs` (ডিপেন্ডেন্সি নেই), `xss-test.mjs` (playwright-core লাগবে)।

## ৬. তৃতীয় রাউন্ড — CSRF, লগইন রেট-লিমিট, MCQ কোটা (সম্পন্ন)

### CSRF — সবচেয়ে বড় ঝুঁকিটি ছিল এখানে
সেশন কুকি **`SameSite=None`** — অর্থাৎ ক্রস-সাইট POST/PUT/DELETE-এও কুকি পাঠানো হয়।
ফলে যেকোনো তৃতীয় পক্ষের সাইট একজন লগইন-করা এডমিনের ব্রাউজার দিয়ে যেকোনো অ্যাকশন
চালাতে পারতো (ইউজার যোগ/রোল বদল/কনটেন্ট মুছে ফেলা)।

**ফিক্স:** গ্লোবাল মিডলওয়্যার — state-changing মেথডে `Origin` অবশ্যই `Host`-এর সাথে মিলতে হবে।
সার্ভার-টু-সার্ভার কলে `Origin` থাকে না, তাই সেগুলো ছাড় (bKash কলব্যাক GET, কোনো প্রভাব নেই)।

যাচাই (প্রোডাকশন): `Origin: https://evil.example.com` → **HTTP 403** · নিজের Origin → 200 · GET অপ্রভাবিত।

### লগইন রেট-লিমিট
৮ বার ভুল চেষ্টার পর ১০ মিনিটের লক (IP + identifier)। সফল লগইনে কাউন্টার রিসেট।

### MCQ প্রশ্নব্যাংক কোটা
`/api/tools/mcq/quiz`-এ আগে কোনো সীমাই ছিল না — যে কেউ ২০টি করে তুলে পুরো ব্যাংক স্ক্র্যাপ করতে পারতো
(`correct` ফিল্ড বাদ থাকলেও)। হার্ড অথ-গার্ড দিলে অনামা ফ্রি প্র্যাকটিস বন্ধ হয়ে যায় (কনভার্শন ফানেল),
তাই **দৈনিক কোটা**: অতিথি ৪০/দিন (১০/রিকোয়েস্ট) · লগইনকৃত ৪০০/দিন (২০/রিকোয়েস্ট)।
কোটা শেষ হলে জেনেরিক এরর নয় — **লগইন CTA-সহ ইন-পেইজ নোটিশ** (`textContent`, তাই XSS-মুক্ত)।

### ⚠️ সীমাবদ্ধতা — সৎকথা
রেট-লিমিট ও কোটা দুটিই **ইন-মেমোরি**, আর Cloudflare রিকোয়েস্ট একাধিক isolate-এ ছড়িয়ে দেয়।
ফলে কাউন্টার isolate-ভিত্তিক — **বেস্ট-এফোর্ট**:

| | স্থানীয় | প্রোডাকশন |
|---|---|---|
| লগইন লক | ঠিক ৯ম চেষ্টায় | ৯ম বা ১০ম (isolate ভেদে) |
| MCQ কোটা | ঠিক ৪০-এ বন্ধ | ৪০-এ বন্ধ (কিছু ক্ষেত্রে সামান্য বেশি) |

CSRF গার্ড কিন্তু **stateless** — এটি সব জায়গায় নির্ভুলভাবে কাজ করে।

**টেকসই করতে চাইলে (আপনি ড্যাশবোর্ডে করতে পারবেন):**
- Cloudflare → Security → WAF → **Rate limiting rules** — `/api/auth/login`-এ ১০/মিনিট,
  `/api/tools/mcq/quiz`-এ ২০/মিনিট। এটি edge-ব্যাপী ও টেকসই।
- অথবা Workers **Rate Limiting binding** (`wrangler.jsonc`-এ `ratelimits`) — কোড-লেভেল ও টেকসই।

যাচাই: `sec-test.mjs` → প্রোডাকশন ও লোকাল উভয়েই **সব পাস**।

## ৭. চতুর্থ রাউন্ড — লকড কনটেন্টে কনটেক্সচুয়াল আপসেল CTA (সম্পন্ন)

**সমস্যা:** সব লকড প্রশ্নপত্র/সাজেশনে একই জেনেরিক **"আনলক"** লিংক ছিল, যা `/subscription`-এর
সাধারণ পেইজে পাঠাতো। কনভার্শনের ঠিক যে মুহূর্তে ইউজার জানতে চায় *"কোন প্ল্যান নিলে এইটুকু খুলবে?"* —
সেই তথ্যটিই অনুপস্থিত ছিল।

**এখন যা দেখায়:**

| | আগে | এখন |
|---|---|---|
| বাটনের লেখা | `🔒 আনলক` | `🔒 প্রিমিয়াম দিয়ে আনলক` |
| লিংক | `/subscription` | `/subscription?plan=premium&from=qp` |
| প্ল্যান-নির্দিষ্ট রঙ | নেই | প্রিমিয়াম = আম্বার, স্ট্যান্ডার্ড = স্কাই |
| ল্যান্ডিংয়ে | সাধারণ পেইজ | প্রয়োজনীয় প্ল্যান **হাইলাইট + স্ক্রল + ব্যানার** |

ব্যানার: *"আপনি যে কনটেন্টটি দেখছিলেন সেটি প্রিমিয়াম প্যাকেজে পাবেন — নিচে হাইলাইট করা প্ল্যানটি দেখুন।"*

**পথে যে বাগ ধরা পড়ল:** `/subscription` পেইজে `ACCESS` কনস্ট্যান্ট নেই (সেটি শুধু `/qpapers`-এ আছে),
তাই প্রথম চেষ্টায় `ReferenceError` দিচ্ছিল — লোকাল `PLAN_BN` ম্যাপ দিয়ে মিটানো হয়েছে।

**যাচাই:** `upsell-test.mjs` → লোকাল ও প্রোডাকশন উভয়েই **৮/৮ পাস**
(৩টি লকড CTA-তেই প্ল্যান-নির্দিষ্ট লিংক · `#plan-card-premium` হাইলাইট · ব্যানার · ০ JS এরর)।

> প্রোডাকশনে পরীক্ষার জন্য একটি অস্থায়ী অ্যাকাউন্ট তৈরি করে কাজ শেষে **মুছে ফেলা হয়েছে**
> (`wrangler d1 execute`, ২২টি রিলেটেড টেবিল থেকে চাইল্ড-রো সহ) — ইউজার সংখ্যা আবার ৪।

## ৮. পঞ্চম রাউন্ড — অ্যাডমিন লেজি ট্যাব + প্রোডাকশন D1 স্কিমা-ড্রিফট (সম্পন্ন)

### ৮.১ ট্যাব-পেইন লেজি হাইড্রেশন — DOM নোড ১,১২৮ → ~৫০০

আগে ১৮টি ট্যাব-পেইনই সার্ভার থেকে DOM-এ বসানো হতো, যদিও ইউজার এক সময়ে একটিই দেখে।
এখন অ্যাকটিভ পেইন ছাড়া বাকি সব `<template>`-এ ইনার্ট থাকে — `<template>` পার্স হলেও
**DOM-নোড তৈরি করে না**। প্রথমবার ওই ট্যাবে গেলেই `ensureTabPane()` সেটিকে DOM-এ আনে,
টেমপ্লেটের ঠিক আগে ঢোকানো হয় ফলে ডকুমেন্ট-অর্ডার অটুট থাকে।

| | আগে | এখন |
|---|---|---|
| প্রাথমিক DOM নোড | ১,১২৮ | **৫০৯** (লোকাল) · **৪৫৮** (প্রোডাকশন) |
| DOM-এ পেইন | ১৮ | ১ (অ্যাকটিভ) |
| `<template>`-এ পার্কড | ০ | ১৭ |

সব ১৭টি ট্যাব হাইড্রেট ও প্রদর্শিত হয় — যাচাইকৃত, ০ JS এরর।

### ৮.২ 🔴 প্রোডাকশনে লুকিয়ে থাকা বড় সমস্যা — স্কিমা-ড্রিফট

লেজি-ট্যাব টেস্ট চালানোর সময় ধরা পড়ে: **`/admin`-এ ৪টি এন্ডপয়েন্ট ৫০০ দিচ্ছিল** (লোকালে ২০০)।

```
/api/admin/sync-sources · /api/admin/sync-logs · /api/admin/audit-logs · /api/teacher-support/mentors
```

**দুটি আলাদা কারণ:**

| কারণ | বিস্তারিত |
|---|---|
| অনুপস্থিত **টেবিল** | `sync_sources`, `sync_logs`, `admin_audit_logs`, `mentor_payouts` — এগুলোর কোনো migration ফাইল নেই, শুধু `ensureD1Schema()`-এ আছে, আর সেটি প্রোডাকশনে কাজ করেনি। ফলে **সিঙ্ক সেন্টার ও অডিট লগ সম্পূর্ণ অচল** ছিল। |
| অনুপস্থিত **কলাম** | `CREATE TABLE IF NOT EXISTS` দিয়ে বিদ্যমান টেবিলের কলাম-ঘাটতি কখনোই ঠিক হয় না। `teachers.is_active` না থাকায় `WHERE is_active=1` ব্যর্থ → **মেন্টর সাপোর্ট ৫০০**। `admissions`/`question_papers`/`scholarships`-এ `source` নেই → লিস্ট/CRUD ব্যর্থ। |

**যা করা হয়েছে**

1. প্রোডাকশন D1-এ সরাসরি ৪টি টেবিল তৈরি + ৪টি কলাম যোগ (`ALTER TABLE ... ADD COLUMN`)
2. **রুট-কজ ফিক্স:** `ensureD1Columns()` — পরিচিত কলামগুলো idempotent-ভাবে যোগ করে,
   ফলে ভবিষ্যতে এই ধরনের ড্রিফট **নিজে থেকেই সেরে যায়**
3. **নতুন টুল:** `schema-drift.mjs` — প্রোডাকশন D1 বনাম লোকাল স্কিমা তুলনা করে
   কলাম-লেভেল ড্রিফট ধরে (এবারের ৪টিই এর মাধ্যমে পাওয়া)

**যাচাই:** প্রোডাকশনে ৪৪টি টেবিল জুড়ে কলাম-ড্রিফট **০** · ৪টি এন্ডপয়েন্ট **২০০** ·
`lazytab-test` প্রোডাকশনে সব পাস।

> 💡 এই সমস্যাটি ধরা পড়েছিল একমাত্র কারণে: আমি লেজি-ট্যাব টেস্টে **প্রতিটি ট্যাব সত্যিই
> ভিজিট** করেছিলাম। শুধু পেইজ লোড করে HTTP ২০০ দেখলে কখনোই ধরা পড়তো না।

## ৯. ষষ্ঠ রাউন্ড — অনবোর্ডিং চেকলিস্ট + ওয়ালেট বোনাস (সম্পন্ন)

**কী:** ড্যাশবোর্ডে (হিরোর ঠিক নিচে) একটি প্রগ্রেস-চালিত "শুরুর ৫ ধাপ" কার্ড।
প্রতিটি ধাপ শেষ করলে বোনাস **ক্লেইম** করা যায় — টাকা সাথে সাথে ওয়ালেটে যোগ হয়।

| ধাপ | বোনাস | "সম্পন্ন" যাচাই হয় |
|---|---|---|
| প্রোফাইলের মূল তথ্য | ৳২০ | `father_bn` + `mother_bn` + `dob` + `upazila` + `district` |
| ছবি ও স্বাক্ষর আপলোড | ৳১৫ | `photo_data` + `sign_data` |
| শিক্ষা তথ্য যোগ | ৳১৫ | (SSC বোর্ড+রোল) বা (HSC বোর্ড+রোল) বা `nu_reg` |
| প্রথম রেজাল্ট সংরক্ষণ | ৳১০ | `saved_rolls` ≥ ১ |
| প্রথম MCQ টেস্ট | ৳১০ | `mcq_attempts` ≥ ১ |
| **মোট** | **৳৭০** | |

### ৯.১ UX — কীভাবে কাজ করে

- **তিনটি দশা:** বাকি → `border-white/10` + CTA লিংক · সম্পন্ন → `border-amber-400/40` +
  জ্বলজ্বলে **ক্লেইম** বাটন · ক্লেইমড → `border-emerald-500/25` + ✓ + "পেয়েছেন"
- **প্রগ্রেস বার** ক্লেইম-ভিত্তিক (কতগুলো ধাপের টাকা হাতে পেলেন), ডানে `২/৫` ও `৳৩৫ পেয়েছেন`
- পেন্ডিং বোনাস থাকলে নিচে **"৳২০ বোনাস ক্লেইম করুন"** — এক ক্লিকে সব
- ক্লেইমের সাথে সাথে হেডার/সাইডবারের **ওয়ালেট ব্যালেন্সও আপডেট** হয়
- **সব ধাপ শেষ + সব ক্লেইম → কার্ড নিজেই সরে যায়** (এবং আর ফিরে আসে না)
- পেন্ডিং টাকা থাকলে X দিয়ে ডিসমিস করলেও পরের বার **কার্ড ফিরে আসে** — টাকা হাতছাড়া হবে না

### ৯.২ 🔒 টাকা দেওয়ার নিয়ম — ডবল-ক্রেডিট ইম্পসিবল

1. ধাপ "সম্পন্ন" কিনা **সার্ভারই** ঠিক করে (`profiles` / `saved_rolls` / `mcq_attempts`
   থেকে)। ক্লায়েন্ট শুধু `{key}` পাঠায় — ক্লায়েন্টের দাবিতে কোনো টাকা পাওয়া যায় না।
2. নতুন টেবিল `onboarding_rewards` — **`UNIQUE (user_id, step_key)`**।
   `INSERT OR IGNORE` চালিয়ে `changes === 0` এলে বোঝা যায় আগেই ক্লেইম করা হয়েছে →
   **ক্রেডিট নেই**। টেবিল/কোয়েরি ব্যর্থ হলেও ক্রেডিট হয় না।
3. ওয়ালেট আপডেট + `wallet_transactions (type='onboarding')` একই সাথে; ওয়ালেট ব্যর্থ হলে
   রিওয়ার্ড রো থাকলেও টাকা যোগ হয় না।
4. `requireAuth` — নিজের বোনাস ছাড়া কারোর ক্লেইম করা যায় না।

**মাইগ্রেশন:** `migrations/0019_onboarding_rewards.sql` + `ensureD1Schema()`-এ
`CREATE TABLE IF NOT EXISTS` — তাই প্রোডাকশনে টেবিল **নিজে তৈরি হয়ে যায়**, আলাদা করে
কিছু চালাতে হবে না।
ওয়ালেট পেজের লেনদেন তালিকায় এগুলো **"🎁 অনবোর্ডিং বোনাস"** হিসেবে দেখা যায়।

### ৯.৩ যাচাই — `onboarding-test.mjs` (৩০ চেক, সব পাস)

রিয়েল ফ্লো: নতুন সাইনআপ → ০/৫ → প্রোফাইল PUT, সেভড-রোল POST, MCQ সাবমিট → ৫/৫ ও
পেন্ডিং ৳৭০ → UI-তে ক্লেইম → **ওয়ালেট ঠিক ৭০ বাড়ল** → **আবার ক্লেইমে ০ (idempotent)** →
রিলোডেও কার্ড ফিরে আসেনি। দ্বিতীয় ইউজার দিয়ে আংশিক অবস্থা (১/৫, পেন্ডিং ৳২০) যাচাই।

```
✅ ALL PASS   ·  JS ত্রুটি: 0
```

**লেআউট:** ডেস্কটপ কার্ড ২৯৪px · মোবাইল (৩৯০px) ৪৩০px · সারি ৫১px · কোথাও
হরাইজন্টাল ওভারফ্লো নেই · বাংলা লেখা কোথাও ক্লিপ/ট্রাঙ্কেট হয়নি।

## ১০. সপ্তম রাউন্ড — ⌘K কমান্ড প্যালেট (সম্পন্ন)

**কী:** যেকোনো পেজ থেকে **Ctrl/⌘ + K** (বা `/`, বা হেডারের 🔍 বোতাম) — পেজ, টুল ও কাজ
এক জায়গায় খোঁজা যায়। `pageShell()`-এ থাকায় সাইটের **প্রতিটি পেজেই** কাজ করে;
ড্যাশবোর্ড ও অ্যাডমিন প্যানেলের নিজস্ব হেডারেও ট্রিগার বোতাম যোগ করা হয়েছে।

**৩০টি কমান্ড, ৪টি গ্রুপ:** পেজ (১৭) · আমার অ্যাকাউন্ট · কাজ · প্রশাসন।

| কাজ | কীভাবে |
|---|---|
| খোলা | `Ctrl/⌘ + K`, অথবা `/` (ইনপুটে থাকলে নয়), অথবা হেডারের 🔍 |
| খোঁজা | বাংলা **ও** ইংরেজি দুটোতেই — `mcq` লিখলে "MCQ পরীক্ষা", `wallet` লিখলে "আমার ওয়ালেট" |
| চলা | `↑`/`↓` (স্ক্রল স্বয়ংক্রিয়ভাবে অনুসরণ করে) · `Enter` নির্বাচন · `Esc` বন্ধ · ব্যাকড্রপে ক্লিক |
| বুদ্ধিমত্তা | `/api/me` দিয়ে বোঝে লগইন/অ্যাডমিন কি না → গেস্টের কাছে "আমার ওয়ালেট"/"প্রশাসন" **লুকানো**, বদলে "লগইন"/"ফ্রি সাইন-আপ" দেখা যায় |

**কাজ-কমান্ডগুলো পেজ-সচেতন:** ড্যাশবোর্ডে থাকলে "রেজাল্ট দেখুন" সরাসরি রেজাল্ট মোডাল
খোলে, অন্য পেজ থেকে `/results`-এ নিয়ে যায়। "ওয়ালেটে টাকা যোগ করুন" ড্যাশবোর্ডে
ক্যাশ-ইন মোডাল, অন্যত্র `/wallet`। "অনবোর্ডিং বোনাস ক্লেইম" সরাসরি ষষ্ঠ রাউন্ডের
`claimOnboarding()`-কে কল করে — দুই রাউন্ড একসাথে কাজ করে।

**নিরাপত্তা/অ্যাক্সেসিবিলিটি:** সব টেক্সট `textContent` দিয়ে বসানো (কোটেশন বা ইউজার-ডেটা
দিয়ে HTML ভাঙার সুযোগ নেই) · `role="dialog"` + `aria-modal` + `aria-activedescendant` ·
খোলার সময় পেছনের স্ক্রল লক, বন্ধে ফোকাস আগের জায়গায় ফেরত · ইনপুট ফন্ট ১৬px (iOS জুম বন্ধ)।

**যাচাই — `cmd-palette-test.mjs` (৩১ চেক, সব পাস):** খোলা/বন্ধ, ফোকাস, স্ক্রল-লক,
বাংলা+ইংরেজি সার্চ, ↑↓ নেভিগেশন, Enter-এ সত্যি নেভিগেশন, গেস্ট বনাম অ্যাডমিন কমান্ড,
মোবাইল ৩৯০px-এ ওভারফ্লো ০ ও ট্যাপ-টার্গেট ≥ ৪৪px, JS ত্রুটি ০।

```
✅ ALL PASS
```

**খরচ:** প্রতি পেজে +১৯ DOM নোড (ড্যাশবোর্ড ৭২২ → ৭৪১)। নতুন কোনো 3rd-party রিকোয়েস্ট নেই।

## ১১. অষ্টম রাউন্ড — nonce-ভিত্তিক Content-Security-Policy (সম্পন্ন)

আগে `_headers`-এ লেখা ছিল: "অ্যাপে ১২০+ ইনলাইন `<script>` ব্লক আছে, তাই কড়া CSP সব
ভেঙে দেবে"। সেই কারণটিই এখন দূর করা হয়েছে।

**কীভাবে:** প্রতি রিকোয়েস্টে একটি নতুন nonce তৈরি হয় (`crypto.getRandomValues`),
রেন্ডার শেষে রেসপন্স HTML-এর **প্রতিটি** `<script>` ট্যাগে `nonce="…"` বসানো হয়,
এবং `Content-Security-Policy` হেডারে `'nonce-…'` পাঠানো হয়। রেন্ডার-পরবর্তী
ইনজেকশন হওয়ায় **কোনো পেজ ফাইলেই হাত দিতে হয়নি** — ভবিষ্যতে নতুন পেজ যোগ হলেও
সেটি নিজে থেকেই nonce পেয়ে যাবে।

```
default-src 'self';  script-src 'self' 'nonce-…';  script-src-attr 'unsafe-inline';
style-src 'self' 'unsafe-inline';  img-src 'self' data: blob: https:;
font-src 'self' data:;  connect-src 'self';  frame-src 'self' blob: data: about:;
worker-src 'self';  manifest-src 'self';  base-uri 'self';  form-action 'self';
frame-ancestors 'self';  object-src 'none';  upgrade-insecure-requests
```

**এতে কী বন্ধ হলো:** ইনজেক্টেড `<script>` ট্যাগ (XSS-এর প্রধান পথ) · 3rd-party
স্ক্রিপ্ট লোড · `javascript:`-নয় এমন সব বাহ্যিক এক্সিকিউশন।

### ⚠️ সৎ সীমাবদ্ধতা — যা এখনো রক্ষা করে না

অ্যাপজুড়ে ইনলাইন ইভেন্ট-হ্যান্ডলার (`onclick="…"`) আছে — সেগুলো **nonce দিয়ে কভার করা
যায় না**, তাই `script-src-attr 'unsafe-inline'` রাখতে হয়েছে। অর্থাৎ কেউ যদি কোনোভাবে
একটি `on…` অ্যাট্রিবিউট ইনজেক্ট করতে পারে, এই CSP সেখানে বাধা দেবে না। মূল প্রতিরক্ষা
তখনো `escHtml()`-ভিত্তিক সার্ভার-সাইড এস্কেপিং — যা চতুর্থ রাউন্ড থেকে কার্যকর
(`xss-test.mjs` সবসময় পাস করে)। হ্যান্ডলারগুলো `addEventListener`-এ রূপান্তর করলে
কেবল `script-src-attr`-ও সরানো যাবে।

### যাচাই — `csp-test.mjs`

* nonce প্রতি রিকোয়েস্টে বদলায় · সব `<script>` ট্যাগে nonce বসে (৬/৬ বা তার বেশি)
* **৩০টি রুট** × প্লেয়ারাইট: CSP ভায়োলেশন **০**, JS এরর **০**, ইনলাইন স্ক্রিপ্ট সত্যিই চলছে
* **নেগেটিভ কন্ট্রোল:** nonce ছাড়া `<script>` ইনজেক্ট করলে সেটি **চলে না** এবং ব্রাউজার
  `script-src-elem ← inline` ভায়োলেশন রিপোর্ট করে — টেস্টটি অর্থহীন হয়ে যায় না
* `https://example.com/...` স্ক্রিপ্ট লোড **ব্লক**
* পিডিএফ ডাউনলোডের hidden `about:blank` iframe (`/qpapers`-এ সত্যিকারের ক্লিক দিয়ে পরীক্ষা)
  — ভায়োলেশন ০

```
✅ ALL PASS
```

## ১২. পরের ধাপ (রোডম্যাপ)

- **H3** টেকসই রেট-লিমিটিং — Cloudflare WAF rule বা Workers Rate Limiting binding

## ১৩. লোকাল প্রিভিউ

**http://localhost:3000** — `npx tsx server.ts` (চালু আছে)

| রোল | ফোন | পাসওয়ার্ড |
|---|---|---|
| অ্যাডমিন | `01829486022` | `Ab52944820@` |
| টেস্ট ইউজার | `01911000001` / `01911000002` / `01911000003` | `test1234` |
