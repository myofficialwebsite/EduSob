# 🚀 ডিপ্লয় স্ট্যাটাস — ১০ সেপ্টেম্বর ২০২৬

**লাইভ URL:** https://edusob.pages.dev
**সর্বশেষ ডিপ্লয়:** `e8cd374d` (production, branch `main`)
**GitHub `main`:** `e181829`
**D1 ব্যাকআপ:** `data/edusob-production-2026-09-10.sql` (৩৯৬ KB — গিট-ইগনোর করা, রিপোতে নেই)

---

## ১. কী কী পুশ ও ডিপ্লয় হলো

| কমিট | কী |
|---|---|
| `6d98a50` | অডিট-ভিত্তিক P0 ফিক্স: XSS, সেশন-ডিলিট বাগ, `'\n'` ক্র্যাশ, /results স্পিড, ডিজাইন সিস্টেম v2, মোবাইল ওভারফ্লো |
| `920b107` | `DEPLOY.md` রানবুক |
| `be49246` | ডেটাবেস/কুকিজ গিট-ট্র্যাকিং বন্ধ, অ্যাডমিন পাসওয়ার্ড-রিসেট বাগ ফিক্স, রিপোর্ট থেকে ক্রেডেনশিয়াল রিড্যাক্ট |
| `a121eb6` | সিকিউরিটি হেডার (`_headers`), স্ট্যাটিক ক্যাশিং, ডিপ্লয়-ওয়ার্কফ্লো টেমপ্লেট |
| `11b2aad` | SSR পেইজে সিকিউরিটি হেডার (ওয়ার্কার মিডলওয়্যার) |

> ⚠️ **পুশ ঠিকঠাক হয়েছে, কিন্তু Cloudflare স্বয়ংক্রিয়ভাবে বিল্ড করেনি।**
> কারণ: Pages-এর git-connected GitHub App ট্রিগার বন্ধ — `main`-এ পুশ যাচ্ছে (GitHub-এ `11b2aad` আছে),
> কিন্তু ২০২৬-০৯-০৯-এর পর আর একটাও `github:push` ডিপ্লয় তৈরি হয়নি।
> তাই আমি `wrangler pages deploy` দিয়ে **সরাসরি ডিপ্লয়** করেছি — প্রোডাকশন এখন সর্বশেষ কোডেই আছে।

---

## ২. প্রোডাকশনে যা যাচাই করা হয়েছে ✅

**রাউটসমূহ (সব HTTP 200)** — `/`, `/dashboard`, `/admin`, `/results`, `/qpapers`, `/syllabus`, `/wallet`, `/profile`, `/news`, `/jobs`, `/notices`, `/cv`, `/scholarships`

**ফিচার**

- `/qpapers` ও `/syllabus` — আগে `'\n'` ক্র্যাশে ৫০০ দিত, এখন ২০০ ✅
- `/api/admin/stats` → `stats.trends` (today/yesterday/delta) লাইভ ✅
- `/api/link-status/batch` → ক্যাশ কাজ করছে: ১ম কল `cached=0 checked=5`, ২য় কল `cached=5 checked=0` ✅
- D1 সংযুক্ত ও সাড়া দিচ্ছে (admin লগইন → `role: admin`) ✅

**কর্মদক্ষতা (প্রোডাকশন, ৫টি URL)**

| অবস্থা | সময় |
|---|---|
| কোল্ড (৫টি রিয়াল প্রোব) | ৩.৯৯ সে. |
| ওয়ার্ম (ক্যাশ থেকে) | **০.৩১–০.৬০ সে.** |

**নিরাপত্তা হেডার (লাইভ, সব SSR পেইজে)**

```
x-content-type-options: nosniff
referrer-policy: strict-origin-when-cross-origin
x-frame-options: SAMEORIGIN
permissions-policy: camera=(), microphone=(self), geolocation=(), interest-cohort=()
```

**কোনো CDN নির্ভরতা নেই** — `fonts.googleapis.com: 0`, `jsdelivr: 0`। ফন্ট ও আইকন সব সেলফ-হোস্টেড।

---

## ৩. পুশের আগে যা ঠিক করতে হয়েছিল (রিপো **পাবলিক**)

1. **`data/*.sqlite` ও `cookies.txt` গিটে ট্র্যাকড ছিল** — অর্থাৎ যে কেউ রিপো ক্লোন করলে লাইভ ইউজার-ডেটাবেস পেত।
   এখন `.gitignore`-এ আছে এবং ইনডেক্স থেকে সরানো হয়েছে (ফাইল ডিস্কে আছে, লোকাল ডেভ কাজ করবে)।
2. **অ্যাডমিন পাসওয়ার্ড-রিসেট বাগ** — `initDatabase()`-এর `else` শাখা প্রতিবার বুটে `WHERE role='admin'` দিয়ে সব অ্যাডমিনের পাসওয়ার্ড ওভাররাইট করতো। এখন শুধু নতুন ডেটাবেসে সিড হয়, বিদ্যমান অ্যাডমিনের পাসওয়ার্ড আর ছোঁয় না।
3. **`AUDIT_REPORT.md`-এ প্লেইন-টেক্সট পাসওয়ার্ড** — আমি নিজে লিখেছিলাম, রিড্যাক্ট করা হয়েছে।

---

## ৪. 🔴 আপনাকেই করতে হবে (আমি করতে পারি না)

1. **টোকেন দুটি রিভোক করুন** — কাজ শেষ।
   - GitHub → https://github.com/settings/tokens
   - Cloudflare → https://dash.cloudflare.com/profile/api-tokens
2. ~~**অ্যাডমিন পাসওয়ার্ড বদলুন**~~ — ✅ সম্পন্ন: পাসওয়ার্ড ঘোরানো হয়েছে, প্লেইনটেক্সট কোড থেকে সরানো হয়েছে।
3. **গিট হিস্ট্রি পরিষ্কার করুন** (না হলে `data/` হিস্ট্রিতে থাকবেই):
   ```bash
   git filter-repo --path data/ --path cookies.txt --invert-paths --force
   git push --force
   ```
4. **স্বয়ংক্রিয় ডিপ্লয় চালু করুন** — দুটির যেকোনো একটি:
   - **(ক) সহজ:** Cloudflare Dashboard → Workers & Pages → `edusob` → git সংযোগ আবার লিঙ্ক করুন।
   - **(খ) নির্ভরযোগ্য:** `docs/deploy-workflow.example.yml`-কে `.github/workflows/deploy.yml`-এ কপি করুন,
     আর GitHub → Settings → Secrets-এ `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` যোগ করুন।
     ⚠️ `.github/workflows/**` পুশ করতে PAT-এ **`workflow` স্কোপ** লাগবে — তাই ফাইলটি আপাতত `docs/`-এ টেমপ্লেট হিসেবে রাখা হয়েছে।

---

## ৫. দ্বিতীয় রাউন্ড — stored-XSS বন্ধ + H1 (সম্পন্ন)

**এডমিন প্যানেলে stored-XSS ছিল — প্রমাণিত, ঠিক করা, নেগেটিভ-কন্ট্রোল দিয়ে যাচাই।**

অ্যাডমিন প্যানেল `onclick="openRoleModalById(1, '<নাম>', ...)"` আকারে বাটন বানাতো এবং
ভেতরের মান `esc()` দিয়ে এস্কেপ করতো। কিন্তু `esc()` শুধু `& < > "` এস্কেপ করে — **(`') নয়**।
ব্রাউজার আগে অ্যাট্রিবিউটটি HTML-ডিফোড করে, এরপর JS পার্স করে, তাই একটি সাধারণ `'` দিয়েই
স্ট্রিং ভেঙে কোড ইনজেক্ট করা যায়। সাইনআপ `name_bn`-এ শুধু `<>` ফিল্টার করে, `'` নয়।

**প্রমাণ (রিয়েল ব্রাউজার, Chromium):**

```
সাধারণ সাইনআপ পথেই:  name_bn = "রহিম'); alert('XSS-PWNED'); //"
এডমিন Users ট্যাবে "রোল" বাটনে ক্লিক →
   আগে  : alert('XSS-PWNED') ফায়ার  ❌  (dialogs: ["XSS-PWNED","XSS-PWNED"])
   এখন  : ২৪টি বাটন ক্লিক, ০ alert  ✅  (onclick: 'রহিম\'); alert(\'XSS-PWNED\'); //')
```

**ফিক্স**

| হেল্পার | কাজ |
|---|---|
| `jsq()` | ব্যাকস্ল্যাশ ও `'` কে JS-এস্কেপ → এরপর HTML-এস্কেপ। ৬টি `onclick` কল-সাইটে `esc()` বদলে ব্যবহার |
| `safeUrl()` | `href`-এ `javascript:` / `data:` স্কিম বন্ধ — `esc()` দিয়ে আটকানো যায় না |
| `esc()` | `toastMsg` ও `logBox`-এর আনএস্কেপড ইন্টারপোলেশনে প্রয়োগ |

> নোট: `jsq()`-এ `String.fromCharCode(92)` ব্যবহার করা হয়েছে — সোর্সে কোনো ব্যাকস্ল্যাশ
> লিটারেল নেই। অ্যাডমিনের JS একটি সিঙ্গেল-কোটেড স্ট্রিং-এর ভেতরে এমবেড করা, সেখানে `\n`
> লিখলে আসল নিউলাইনে পরিণত হয়ে রেজেক্স ভেঙে যায় (প্রথম চেষ্টায় সত্যিই ভেঙেছিল)।

**H1 (a11y/SEO):** `/wallet` ও `/admin/shop`-এ কোনো H1 ছিলই না — যোগ করা হয়েছে।
যাচাই: `h1audit.mjs` → **৩২টি রুটেই ঠিক একটি H1** (আগে ২টি রুটে ০, ১টিতে ভুল-পজিটিভ)।

**রিগ্রেশন যাচাই:** `tsc` clean · ১১১ ইনলাইন স্ক্রিপ্ট ব্লক, ০ broken ·
২৪ রুট × ২ ভিউপোর্ট → **০ সমস্যা** (কোনো ওভারফ্লো বা কনসোল-এরর নেই)।

**নতুন টেস্ট টুলিং:** `h1audit.mjs` (ডিপেন্ডেন্সি নেই), `xss-test.mjs` (playwright-core লাগবে)।

## ৬. তৃতীয় রাউন্ড — CSRF, লগইন রেট-লিমিট, MCQ কোটা (সম্পন্ন)

### CSRF — সবচেয়ে বড় ঝুঁকিটি ছিল এখানে
সেশন কুকি **`SameSite=None`** — অর্থাৎ ক্রস-সাইট POST/PUT/DELETE-এও কুকি পাঠানো হয়।
ফলে যেকোনো তৃতীয় পক্ষের সাইট একজন লগইন-করা এডমিনের ব্রাউজার দিয়ে যেকোনো অ্যাকশন
চালাতে পারতো (ইউজার যোগ/রোল বদল/কনটেন্ট মুছে ফেলা)।

**ফিক্স:** গ্লোবাল মিডলওয়্যার — state-changing মেথডে `Origin` অবশ্যই `Host`-এর সাথে মিলতে হবে।
সার্ভার-টু-সার্ভার কলে `Origin` থাকে না, তাই সেগুলো ছাড় (bKash কলব্যাক GET, কোনো প্রভাব নেই)।

যাচাই (প্রোডাকশন): `Origin: https://evil.example.com` → **HTTP 403** · নিজের Origin → 200 · GET অপ্রভাবিত।

### লগইন রেট-লিমিট
৮ বার ভুল চেষ্টার পর ১০ মিনিটের লক (IP + identifier)। সফল লগইনে কাউন্টার রিসেট।

### MCQ প্রশ্নব্যাংক কোটা
`/api/tools/mcq/quiz`-এ আগে কোনো সীমাই ছিল না — যে কেউ ২০টি করে তুলে পুরো ব্যাংক স্ক্র্যাপ করতে পারতো
(`correct` ফিল্ড বাদ থাকলেও)। হার্ড অথ-গার্ড দিলে অনামা ফ্রি প্র্যাকটিস বন্ধ হয়ে যায় (কনভার্শন ফানেল),
তাই **দৈনিক কোটা**: অতিথি ৪০/দিন (১০/রিকোয়েস্ট) · লগইনকৃত ৪০০/দিন (২০/রিকোয়েস্ট)।
কোটা শেষ হলে জেনেরিক এরর নয় — **লগইন CTA-সহ ইন-পেইজ নোটিশ** (`textContent`, তাই XSS-মুক্ত)।

### ⚠️ সীমাবদ্ধতা — সৎকথা
রেট-লিমিট ও কোটা দুটিই **ইন-মেমোরি**, আর Cloudflare রিকোয়েস্ট একাধিক isolate-এ ছড়িয়ে দেয়।
ফলে কাউন্টার isolate-ভিত্তিক — **বেস্ট-এফোর্ট**:

| | স্থানীয় | প্রোডাকশন |
|---|---|---|
| লগইন লক | ঠিক ৯ম চেষ্টায় | ৯ম বা ১০ম (isolate ভেদে) |
| MCQ কোটা | ঠিক ৪০-এ বন্ধ | ৪০-এ বন্ধ (কিছু ক্ষেত্রে সামান্য বেশি) |

CSRF গার্ড কিন্তু **stateless** — এটি সব জায়গায় নির্ভুলভাবে কাজ করে।

**টেকসই করতে চাইলে (আপনি ড্যাশবোর্ডে করতে পারবেন):**
- Cloudflare → Security → WAF → **Rate limiting rules** — `/api/auth/login`-এ ১০/মিনিট,
  `/api/tools/mcq/quiz`-এ ২০/মিনিট। এটি edge-ব্যাপী ও টেকসই।
- অথবা Workers **Rate Limiting binding** (`wrangler.jsonc`-এ `ratelimits`) — কোড-লেভেল ও টেকসই।

যাচাই: `sec-test.mjs` → প্রোডাকশন ও লোকাল উভয়েই **সব পাস**।

## ৭. চতুর্থ রাউন্ড — লকড কনটেন্টে কনটেক্সচুয়াল আপসেল CTA (সম্পন্ন)

**সমস্যা:** সব লকড প্রশ্নপত্র/সাজেশনে একই জেনেরিক **"আনলক"** লিংক ছিল, যা `/subscription`-এর
সাধারণ পেইজে পাঠাতো। কনভার্শনের ঠিক যে মুহূর্তে ইউজার জানতে চায় *"কোন প্ল্যান নিলে এইটুকু খুলবে?"* —
সেই তথ্যটিই অনুপস্থিত ছিল।

**এখন যা দেখায়:**

| | আগে | এখন |
|---|---|---|
| বাটনের লেখা | `🔒 আনলক` | `🔒 প্রিমিয়াম দিয়ে আনলক` |
| লিংক | `/subscription` | `/subscription?plan=premium&from=qp` |
| প্ল্যান-নির্দিষ্ট রঙ | নেই | প্রিমিয়াম = আম্বার, স্ট্যান্ডার্ড = স্কাই |
| ল্যান্ডিংয়ে | সাধারণ পেইজ | প্রয়োজনীয় প্ল্যান **হাইলাইট + স্ক্রল + ব্যানার** |

ব্যানার: *"আপনি যে কনটেন্টটি দেখছিলেন সেটি প্রিমিয়াম প্যাকেজে পাবেন — নিচে হাইলাইট করা প্ল্যানটি দেখুন।"*

**পথে যে বাগ ধরা পড়ল:** `/subscription` পেইজে `ACCESS` কনস্ট্যান্ট নেই (সেটি শুধু `/qpapers`-এ আছে),
তাই প্রথম চেষ্টায় `ReferenceError` দিচ্ছিল — লোকাল `PLAN_BN` ম্যাপ দিয়ে মিটানো হয়েছে।

**যাচাই:** `upsell-test.mjs` → লোকাল ও প্রোডাকশন উভয়েই **৮/৮ পাস**
(৩টি লকড CTA-তেই প্ল্যান-নির্দিষ্ট লিংক · `#plan-card-premium` হাইলাইট · ব্যানার · ০ JS এরর)।

> প্রোডাকশনে পরীক্ষার জন্য একটি অস্থায়ী অ্যাকাউন্ট তৈরি করে কাজ শেষে **মুছে ফেলা হয়েছে**
> (`wrangler d1 execute`, ২২টি রিলেটেড টেবিল থেকে চাইল্ড-রো সহ) — ইউজার সংখ্যা আবার ৪।

## ৮. পঞ্চম রাউন্ড — অ্যাডমিন লেজি ট্যাব + প্রোডাকশন D1 স্কিমা-ড্রিফট (সম্পন্ন)

### ৮.১ ট্যাব-পেইন লেজি হাইড্রেশন — DOM নোড ১,১২৮ → ~৫০০

আগে ১৮টি ট্যাব-পেইনই সার্ভার থেকে DOM-এ বসানো হতো, যদিও ইউজার এক সময়ে একটিই দেখে।
এখন অ্যাকটিভ পেইন ছাড়া বাকি সব `<template>`-এ ইনার্ট থাকে — `<template>` পার্স হলেও
**DOM-নোড তৈরি করে না**। প্রথমবার ওই ট্যাবে গেলেই `ensureTabPane()` সেটিকে DOM-এ আনে,
টেমপ্লেটের ঠিক আগে ঢোকানো হয় ফলে ডকুমেন্ট-অর্ডার অটুট থাকে।

| | আগে | এখন |
|---|---|---|
| প্রাথমিক DOM নোড | ১,১২৮ | **৫০৯** (লোকাল) · **৪৫৮** (প্রোডাকশন) |
| DOM-এ পেইন | ১৮ | ১ (অ্যাকটিভ) |
| `<template>`-এ পার্কড | ০ | ১৭ |

সব ১৭টি ট্যাব হাইড্রেট ও প্রদর্শিত হয় — যাচাইকৃত, ০ JS এরর।

### ৮.২ 🔴 প্রোডাকশনে লুকিয়ে থাকা বড় সমস্যা — স্কিমা-ড্রিফট

লেজি-ট্যাব টেস্ট চালানোর সময় ধরা পড়ে: **`/admin`-এ ৪টি এন্ডপয়েন্ট ৫০০ দিচ্ছিল** (লোকালে ২০০)।

```
/api/admin/sync-sources · /api/admin/sync-logs · /api/admin/audit-logs · /api/teacher-support/mentors
```

**দুটি আলাদা কারণ:**

| কারণ | বিস্তারিত |
|---|---|
| অনুপস্থিত **টেবিল** | `sync_sources`, `sync_logs`, `admin_audit_logs`, `mentor_payouts` — এগুলোর কোনো migration ফাইল নেই, শুধু `ensureD1Schema()`-এ আছে, আর সেটি প্রোডাকশনে কাজ করেনি। ফলে **সিঙ্ক সেন্টার ও অডিট লগ সম্পূর্ণ অচল** ছিল। |
| অনুপস্থিত **কলাম** | `CREATE TABLE IF NOT EXISTS` দিয়ে বিদ্যমান টেবিলের কলাম-ঘাটতি কখনোই ঠিক হয় না। `teachers.is_active` না থাকায় `WHERE is_active=1` ব্যর্থ → **মেন্টর সাপোর্ট ৫০০**। `admissions`/`question_papers`/`scholarships`-এ `source` নেই → লিস্ট/CRUD ব্যর্থ। |

**যা করা হয়েছে**

1. প্রোডাকশন D1-এ সরাসরি ৪টি টেবিল তৈরি + ৪টি কলাম যোগ (`ALTER TABLE ... ADD COLUMN`)
2. **রুট-কজ ফিক্স:** `ensureD1Columns()` — পরিচিত কলামগুলো idempotent-ভাবে যোগ করে,
   ফলে ভবিষ্যতে এই ধরনের ড্রিফট **নিজে থেকেই সেরে যায়**
3. **নতুন টুল:** `schema-drift.mjs` — প্রোডাকশন D1 বনাম লোকাল স্কিমা তুলনা করে
   কলাম-লেভেল ড্রিফট ধরে (এবারের ৪টিই এর মাধ্যমে পাওয়া)

**যাচাই:** প্রোডাকশনে ৪৪টি টেবিল জুড়ে কলাম-ড্রিফট **০** · ৪টি এন্ডপয়েন্ট **২০০** ·
`lazytab-test` প্রোডাকশনে সব পাস।

> 💡 এই সমস্যাটি ধরা পড়েছিল একমাত্র কারণে: আমি লেজি-ট্যাব টেস্টে **প্রতিটি ট্যাব সত্যিই
> ভিজিট** করেছিলাম। শুধু পেইজ লোড করে HTTP ২০০ দেখলে কখনোই ধরা পড়তো না।

## ৯. ষষ্ঠ রাউন্ড — অনবোর্ডিং চেকলিস্ট + ওয়ালেট বোনাস (সম্পন্ন)

**কী:** ড্যাশবোর্ডে (হিরোর ঠিক নিচে) একটি প্রগ্রেস-চালিত "শুরুর ৫ ধাপ" কার্ড।
প্রতিটি ধাপ শেষ করলে বোনাস **ক্লেইম** করা যায় — টাকা সাথে সাথে ওয়ালেটে যোগ হয়।

| ধাপ | বোনাস | "সম্পন্ন" যাচাই হয় |
|---|---|---|
| প্রোফাইলের মূল তথ্য | ৳২০ | `father_bn` + `mother_bn` + `dob` + `upazila` + `district` |
| ছবি ও স্বাক্ষর আপলোড | ৳১৫ | `photo_data` + `sign_data` |
| শিক্ষা তথ্য যোগ | ৳১৫ | (SSC বোর্ড+রোল) বা (HSC বোর্ড+রোল) বা `nu_reg` |
| প্রথম রেজাল্ট সংরক্ষণ | ৳১০ | `saved_rolls` ≥ ১ |
| প্রথম MCQ টেস্ট | ৳১০ | `mcq_attempts` ≥ ১ |
| **মোট** | **৳৭০** | |

### ৯.১ UX — কীভাবে কাজ করে

- **তিনটি দশা:** বাকি → `border-white/10` + CTA লিংক · সম্পন্ন → `border-amber-400/40` +
  জ্বলজ্বলে **ক্লেইম** বাটন · ক্লেইমড → `border-emerald-500/25` + ✓ + "পেয়েছেন"
- **প্রগ্রেস বার** ক্লেইম-ভিত্তিক (কতগুলো ধাপের টাকা হাতে পেলেন), ডানে `২/৫` ও `৳৩৫ পেয়েছেন`
- পেন্ডিং বোনাস থাকলে নিচে **"৳২০ বোনাস ক্লেইম করুন"** — এক ক্লিকে সব
- ক্লেইমের সাথে সাথে হেডার/সাইডবারের **ওয়ালেট ব্যালেন্সও আপডেট** হয়
- **সব ধাপ শেষ + সব ক্লেইম → কার্ড নিজেই সরে যায়** (এবং আর ফিরে আসে না)
- পেন্ডিং টাকা থাকলে X দিয়ে ডিসমিস করলেও পরের বার **কার্ড ফিরে আসে** — টাকা হাতছাড়া হবে না

### ৯.২ 🔒 টাকা দেওয়ার নিয়ম — ডবল-ক্রেডিট ইম্পসিবল

1. ধাপ "সম্পন্ন" কিনা **সার্ভারই** ঠিক করে (`profiles` / `saved_rolls` / `mcq_attempts`
   থেকে)। ক্লায়েন্ট শুধু `{key}` পাঠায় — ক্লায়েন্টের দাবিতে কোনো টাকা পাওয়া যায় না।
2. নতুন টেবিল `onboarding_rewards` — **`UNIQUE (user_id, step_key)`**।
   `INSERT OR IGNORE` চালিয়ে `changes === 0` এলে বোঝা যায় আগেই ক্লেইম করা হয়েছে →
   **ক্রেডিট নেই**। টেবিল/কোয়েরি ব্যর্থ হলেও ক্রেডিট হয় না।
3. ওয়ালেট আপডেট + `wallet_transactions (type='onboarding')` একই সাথে; ওয়ালেট ব্যর্থ হলে
   রিওয়ার্ড রো থাকলেও টাকা যোগ হয় না।
4. `requireAuth` — নিজের বোনাস ছাড়া কারোর ক্লেইম করা যায় না।

**মাইগ্রেশন:** `migrations/0019_onboarding_rewards.sql` + `ensureD1Schema()`-এ
`CREATE TABLE IF NOT EXISTS` — তাই প্রোডাকশনে টেবিল **নিজে তৈরি হয়ে যায়**, আলাদা করে
কিছু চালাতে হবে না।
ওয়ালেট পেজের লেনদেন তালিকায় এগুলো **"🎁 অনবোর্ডিং বোনাস"** হিসেবে দেখা যায়।

### ৯.৩ যাচাই — `onboarding-test.mjs` (৩০ চেক, সব পাস)

রিয়েল ফ্লো: নতুন সাইনআপ → ০/৫ → প্রোফাইল PUT, সেভড-রোল POST, MCQ সাবমিট → ৫/৫ ও
পেন্ডিং ৳৭০ → UI-তে ক্লেইম → **ওয়ালেট ঠিক ৭০ বাড়ল** → **আবার ক্লেইমে ০ (idempotent)** →
রিলোডেও কার্ড ফিরে আসেনি। দ্বিতীয় ইউজার দিয়ে আংশিক অবস্থা (১/৫, পেন্ডিং ৳২০) যাচাই।

```
✅ ALL PASS   ·  JS ত্রুটি: 0
```

**লেআউট:** ডেস্কটপ কার্ড ২৯৪px · মোবাইল (৩৯০px) ৪৩০px · সারি ৫১px · কোথাও
হরাইজন্টাল ওভারফ্লো নেই · বাংলা লেখা কোথাও ক্লিপ/ট্রাঙ্কেট হয়নি।

## ১০. সপ্তম রাউন্ড — ⌘K কমান্ড প্যালেট (সম্পন্ন)

**কী:** যেকোনো পেজ থেকে **Ctrl/⌘ + K** (বা `/`, বা হেডারের 🔍 বোতাম) — পেজ, টুল ও কাজ
এক জায়গায় খোঁজা যায়। `pageShell()`-এ থাকায় সাইটের **প্রতিটি পেজেই** কাজ করে;
ড্যাশবোর্ড ও অ্যাডমিন প্যানেলের নিজস্ব হেডারেও ট্রিগার বোতাম যোগ করা হয়েছে।

**৩০টি কমান্ড, ৪টি গ্রুপ:** পেজ (১৭) · আমার অ্যাকাউন্ট · কাজ · প্রশাসন।

| কাজ | কীভাবে |
|---|---|
| খোলা | `Ctrl/⌘ + K`, অথবা `/` (ইনপুটে থাকলে নয়), অথবা হেডারের 🔍 |
| খোঁজা | বাংলা **ও** ইংরেজি দুটোতেই — `mcq` লিখলে "MCQ পরীক্ষা", `wallet` লিখলে "আমার ওয়ালেট" |
| চলা | `↑`/`↓` (স্ক্রল স্বয়ংক্রিয়ভাবে অনুসরণ করে) · `Enter` নির্বাচন · `Esc` বন্ধ · ব্যাকড্রপে ক্লিক |
| বুদ্ধিমত্তা | `/api/me` দিয়ে বোঝে লগইন/অ্যাডমিন কি না → গেস্টের কাছে "আমার ওয়ালেট"/"প্রশাসন" **লুকানো**, বদলে "লগইন"/"ফ্রি সাইন-আপ" দেখা যায় |

**কাজ-কমান্ডগুলো পেজ-সচেতন:** ড্যাশবোর্ডে থাকলে "রেজাল্ট দেখুন" সরাসরি রেজাল্ট মোডাল
খোলে, অন্য পেজ থেকে `/results`-এ নিয়ে যায়। "ওয়ালেটে টাকা যোগ করুন" ড্যাশবোর্ডে
ক্যাশ-ইন মোডাল, অন্যত্র `/wallet`। "অনবোর্ডিং বোনাস ক্লেইম" সরাসরি ষষ্ঠ রাউন্ডের
`claimOnboarding()`-কে কল করে — দুই রাউন্ড একসাথে কাজ করে।

**নিরাপত্তা/অ্যাক্সেসিবিলিটি:** সব টেক্সট `textContent` দিয়ে বসানো (কোটেশন বা ইউজার-ডেটা
দিয়ে HTML ভাঙার সুযোগ নেই) · `role="dialog"` + `aria-modal` + `aria-activedescendant` ·
খোলার সময় পেছনের স্ক্রল লক, বন্ধে ফোকাস আগের জায়গায় ফেরত · ইনপুট ফন্ট ১৬px (iOS জুম বন্ধ)।

**যাচাই — `cmd-palette-test.mjs` (৩১ চেক, সব পাস):** খোলা/বন্ধ, ফোকাস, স্ক্রল-লক,
বাংলা+ইংরেজি সার্চ, ↑↓ নেভিগেশন, Enter-এ সত্যি নেভিগেশন, গেস্ট বনাম অ্যাডমিন কমান্ড,
মোবাইল ৩৯০px-এ ওভারফ্লো ০ ও ট্যাপ-টার্গেট ≥ ৪৪px, JS ত্রুটি ০।

```
✅ ALL PASS
```

**খরচ:** প্রতি পেজে +১৯ DOM নোড (ড্যাশবোর্ড ৭২২ → ৭৪১)। নতুন কোনো 3rd-party রিকোয়েস্ট নেই।

## ১১. অষ্টম রাউন্ড — nonce-ভিত্তিক Content-Security-Policy (সম্পন্ন)

আগে `_headers`-এ লেখা ছিল: "অ্যাপে ১২০+ ইনলাইন `<script>` ব্লক আছে, তাই কড়া CSP সব
ভেঙে দেবে"। সেই কারণটিই এখন দূর করা হয়েছে।

**কীভাবে:** প্রতি রিকোয়েস্টে একটি নতুন nonce তৈরি হয় (`crypto.getRandomValues`),
রেন্ডার শেষে রেসপন্স HTML-এর **প্রতিটি** `<script>` ট্যাগে `nonce="…"` বসানো হয়,
এবং `Content-Security-Policy` হেডারে `'nonce-…'` পাঠানো হয়। রেন্ডার-পরবর্তী
ইনজেকশন হওয়ায় **কোনো পেজ ফাইলেই হাত দিতে হয়নি** — ভবিষ্যতে নতুন পেজ যোগ হলেও
সেটি নিজে থেকেই nonce পেয়ে যাবে।

```
default-src 'self';  script-src 'self' 'nonce-…';  script-src-attr 'unsafe-inline';
style-src 'self' 'unsafe-inline';  img-src 'self' data: blob: https:;
font-src 'self' data:;  connect-src 'self';  frame-src 'self' blob: data: about:;
worker-src 'self';  manifest-src 'self';  base-uri 'self';  form-action 'self';
frame-ancestors 'self';  object-src 'none';  upgrade-insecure-requests
```

**এতে কী বন্ধ হলো:** ইনজেক্টেড `<script>` ট্যাগ (XSS-এর প্রধান পথ) · 3rd-party
স্ক্রিপ্ট লোড · `javascript:`-নয় এমন সব বাহ্যিক এক্সিকিউশন।

### ⚠️ সৎ সীমাবদ্ধতা — যা এখনো রক্ষা করে না

অ্যাপজুড়ে ইনলাইন ইভেন্ট-হ্যান্ডলার (`onclick="…"`) আছে — সেগুলো **nonce দিয়ে কভার করা
যায় না**, তাই `script-src-attr 'unsafe-inline'` রাখতে হয়েছে। অর্থাৎ কেউ যদি কোনোভাবে
একটি `on…` অ্যাট্রিবিউট ইনজেক্ট করতে পারে, এই CSP সেখানে বাধা দেবে না। মূল প্রতিরক্ষা
তখনো `escHtml()`-ভিত্তিক সার্ভার-সাইড এস্কেপিং — যা চতুর্থ রাউন্ড থেকে কার্যকর
(`xss-test.mjs` সবসময় পাস করে)। হ্যান্ডলারগুলো `addEventListener`-এ রূপান্তর করলে
কেবল `script-src-attr`-ও সরানো যাবে।

### যাচাই — `csp-test.mjs`

* nonce প্রতি রিকোয়েস্টে বদলায় · সব `<script>` ট্যাগে nonce বসে (৬/৬ বা তার বেশি)
* **৩০টি রুট** × প্লেয়ারাইট: CSP ভায়োলেশন **০**, JS এরর **০**, ইনলাইন স্ক্রিপ্ট সত্যিই চলছে
* **নেগেটিভ কন্ট্রোল:** nonce ছাড়া `<script>` ইনজেক্ট করলে সেটি **চলে না** এবং ব্রাউজার
  `script-src-elem ← inline` ভায়োলেশন রিপোর্ট করে — টেস্টটি অর্থহীন হয়ে যায় না
* `https://example.com/...` স্ক্রিপ্ট লোড **ব্লক**
* পিডিএফ ডাউনলোডের hidden `about:blank` iframe (`/qpapers`-এ সত্যিকারের ক্লিক দিয়ে পরীক্ষা)
  — ভায়োলেশন ০

```
✅ ALL PASS
```

## ১২. নবম রাউন্ড — টেকসই রেট-লিমিটিং, D1-ভিত্তিক (সম্পন্ন)

**সমস্যা:** লগইন রেট-লিমিটের কাউন্টার ছিল **ইন-মেমোরি**, অর্থাৎ প্রতিটি Worker
isolate-এর নিজস্ব। প্রোডাকশনে রিকোয়েস্ট একাধিক isolate-এ ছড়িয়ে পড়ায় "১০ মিনিটে ৮ বার"
সীমা আসলে **১৬–২৪ বার** হয়ে যেত — `sec-test` নিজেই লোকালে ৯ ও প্রোডাকশনে ১০-১১ দেখিয়েছিল।

**সমাধান:** কাউন্টার এখন D1 `rate_limits` টেবিলে। সব isolate একই সারি দেখে, তাই সীমা
সর্বত্র সমান। খরচ শুধু ব্যর্থ চেষ্টায় একটি লেখা + প্রতি চেষ্টায় একটি পড়া।

| আগে (ইন-মেমোরি) | এখন (D1) |
|---|---|
| isolate-ভিত্তিক — সীমা ২-৩ গুণ হয়ে যেত | **সব isolate-এর জন্য এক হিসাব** |
| প্রসেস/isolate নতুন হলে ০ থেকে শুরু | **রিস্টার্টের পরও সীমা বহাল** |
| — | সফল লগইনে কাউন্টার রিসেট |
| — | IP + আইডেন্টিফায়ার মিলিয়ে বাকেট (এক জনের লকে সবাই আটকে না) |

**আইপি স্পুফিং বন্ধ:** শুধু `CF-Connecting-IP` বিশ্বাস করা হয় (ক্লাউডফ্লেয়ার নিজে সেট করে,
ক্লায়েন্ট বানাতে পারে না)। `X-Forwarded-For` স্পুফ করা যায় — এবং ক্লাউডফ্লেয়ার আসল IP
**সবার শেষে** জোড়ে, তাই ফলব্যাকে শেষ এন্ট্রিটি নেওয়া হয়, প্রথমটি নয়।

**যাচাই — `ratelimit-test.mjs`:** ব্লক হয় **ঠিক ৯ম চেষ্টায়** (আগে ৯-১০ ছিল) ·
দুই ভিন্ন IP-তে আলাদা বাকেট · সফল লগইনের পর কাউন্টার ০ থেকে শুরু ·
টেবিল self-healing (`CREATE TABLE IF NOT EXISTS`, মাইগ্রেশন `0020`)।
রিস্টার্টের পরও সীমা বহাল থাকা হাতে-কলমে দেখা হয়েছে (সার্ভার রিস্টার্টের পরও ৪২৯)।

```
✅ ALL PASS
```

## ১৩. দশম রাউন্ড — অ্যাক্সেসিবিলিটি ও ইউজেবিলিটি (সম্পন্ন)

এই রাউন্ডে কোনো নতুন ফিচার যোগ করা হয়নি — **বিদ্যমান UI-কে WCAG 2.2 AA মানে উন্নীত** করা হয়েছে।
নতুন টুল: `a11y-sweep.mjs` (Playwright-ভিত্তিক অডিটর, ২৪ রুট × ২ ভিউপোর্ট)।

**অডিটর যা যা মাপে**
- WCAG AA কনট্রাস্ট (আলফা কম্পোজিটিং + কার্যকর ব্যাকগ্রাউন্ড স্ট্যাক সহ)
- ট্যাপ টার্গেট ২৪×২৪ CSS px (WCAG 2.2 AA 2.5.8; ইনলাইন লিংক অব্যাহত)
- কীবোর্ড অ্যাক্সেস (`onclick` আছে কিন্তু `tabindex`/`role` নেই)
- নামহীন কন্ট্রোল (`aria-label`/`<label for>`/`placeholder` কোনোটিই নেই)
- ছবির `alt`, হেডিং লেভেল স্কিপ

**মাপা ট্র্যাজেক্টোরি (২৪ রুট × ডেস্কটপ+মোবাইল)**

| ধাপ | কনট্রাস্ট | ট্যাপ | কীবোর্ড | নামহীন | alt | হেডিং |
|---|---|---|---|---|---|---|
| শুরু | ৯৯ | ৩৭ | ১১ | ৬ | ০ | ৪ |
| শেষ | **০** | **০** | **০** | **০** | **০** | **০** |

**মূল সমস্যা ও সমাধান**
1. **সাদা টেক্সট on ব্র্যান্ড অরেঞ্জ/অ্যাম্বার** — `#ffffff on #f97316` = ২.৮:১, `#ffffff on #f59e0b` = ২.১৫:১ (AA-তে দরকার ৪.৫:১)।
   সমাধান: ফিল অপরিবর্তিত রেখে টেক্সট `text-slate-950` — **~৭৩ জায়গা, ১৮+ ফাইল**।
2. **অ্যাডমিন KPI কার্ড কীবোর্ডে অচল** — `div.kpi-card` + `onclick`-এ `tabindex`/`role` ছিল না (১১টি)।
   এখন `role="button" tabindex="0"` + `onkeydown` (Enter/Space)। অ্যাক্সেসিবল নাম কার্ডের নিজস্ব টেক্সট থেকে আসে।
3. **`text-slate-400` সাদা ব্যাকগ্রাউন্ডে** (`#94a3b8 on #ffffff` = ২.৫৬:১) → `text-slate-500`।
   **সতর্কতা:** গাঢ় সারফেসে (`#0f172a`, `#121620`, `#090b0f`) স্লেট-৫০০ আরও খারাপ (৩.৭৫:১) — সেখানে `text-slate-400` ব্যবহার করতে হয়।
   ফাইলভিত্তিক blanket রিপ্লেস নিরাপদ নয় (`cvPages.ts`, `commandOverview.ts` — দুটিতেই আলো-আঁধারি দুই ধরনের সারফেস)।
4. **অ্যাকসেন্ট টেক্সট হালকা** — amber/orange-৬০০→৭০০, rose/pink-৫০০→৬০০, sky-৬০০→৭০০, red-৫০০→৬০০/৭০০।
5. **৯px টেক্সট** — ২৯ জায়গায় `text-[9px]` → `text-[10px]` (বাংলা অক্ষর ৯px-এ পড়া কষ্টকর)।
6. **হেডিং লেভেল স্কিপ** — `/` h4→h2, `/dashboard` h5→h4, `/shop`+`/notices`+`/jobs` h3→h2, `/qpapers` h3→h2, `/syllabus` h4→h3 + কার্ড h3→h2।
7. **ট্যাপ টার্গেট** — ২৪px-এর নিচে থাকা ইনলাইন লিংক/আইকন-বোতামে `py-1.5`/`px-1.5`/`p-2` যোগ (হেল্পলাইন, নোটিশ টিকার, ড্যাশবোর্ড কুইক-লিংক, শপ/নিউজ ন্যাভ, CV কালার সোয়াচ ২০→২৪px)।
8. **নামহীন কন্ট্রোল** — প্রোফাইল ফর্মের ১৫টি ইনপুটে `aria-label` (লেবেলগুলো `<label for>`-এ যুক্ত ছিল না), কপি-বোতাম, ফাইল ইনপুট, ক্যারোসেল ডট, প্ল্যানারের তারিখ ফিল্ড।

**ইচ্ছাকৃত সীমাবদ্ধতা (স্বচ্ছতার জন্য)**
- ১০px টেক্সট **শুধু** ব্যাজ/`kbd`/মাইক্রো-লেবেলে ("EduSob", "AI", "⌘K", নোটিফিকেশন কাউন্ট, "নতুন টুল") — কোনো বডি কপি নয়।
  WCAG-এ সর্বনিম্ন ফন্ট সাইজ বলে কিছু নেই; এগুলো সচেতন ডিজাইন সিদ্ধান্ত।
- ক্যারোসেল ডটের ভিজ্যুয়াল বদল হয়নি (`setSlide()` এখন ভেতরের `span`-এ রং দেয়), ট্যাপ এরিয়া ১০×১০ → ২৪×২৪।

**রিগ্রেশন:** `h1audit` · `scan-inline-js` · `sec-test` · `xss-test` · `upsell-test` ·
`onboarding-test` · `cmd-palette-test` · `lazytab-test` · `csp-test` · `ratelimit-test` ·
`audit-sweep` — **সব ALL PASS**, `audit-sweep` → `ISSUES: none`।
`npm run build` → ১,৬৮৪.৪২ kB (gzip ৩৫৪.১৫ kB)।

## ১৪. একাদশ রাউন্ড — SEO ও শেয়ার ইনফ্রা (সম্পন্ন)

নতুন টুল: `seo-audit.mjs` (robots/sitemap + ২০ রুটের মেটা যাচাই)।

**আগে যা ছিল না — এখন আছে**

| | আগে | এখন |
|---|---|---|
| `/robots.txt` | ৪০৪ | ২০০ (`/admin`, `/api` ও সব লগ-ইন পেজ Disallow) |
| `/sitemap.xml` | ৪০৪ | ২০০ (১৭টি পাবলিক রুট, priority/changefreq সহ) |
| `<link rel="canonical">` | অনুপস্থিত | প্রতি পেজে |
| `og:url`, `og:locale`, `twitter:title/image` | অনুপস্থিত | প্রতি পেজে |
| JSON-LD | অনুপস্থিত | হোমে `EducationalOrganization`+`WebSite`(`SearchAction`), বাকি পেজে `WebPage`+`BreadcrumbList` |
| পেজের নিজস্ব description | **না — সব পেজে একই ১৮৭ অক্ষরের কপি** | ২০টি পেজে ২০টি ইউনিক description (≤১৬০ অক্ষর) |

**কেন এটি গুরুত্বপূর্ণ:** description/og:title সব পেজে একই ছিল, তাই ফেসবুক বা
WhatsApp-এ যেকোনো পেজের লিংক শেয়ার করলে **সবসময় হোমপেজের প্রিভিউ** যেত।

**কাঠামোগত পরিবর্তন (`src/pages/layout.ts`)**
- `HEAD_COMMON` ভেঙে দুটি অংশ: `headMeta(seo, pageTitle)` (পেজভিত্তিক মেটা) + `HEAD_ASSETS` (ফন্ট/CSS/PWA)।
- `pageShell(title, bodyClass, content, extraHead, showFloating, seo?)` — নতুন ষষ্ঠ আর্গুমেন্ট।
  `seo.path` দিলেই `canonical`, `og:url` ও JSON-LD নিজে থেকে তৈরি হয়ে যায় (প্রতি পেজে আলাদা করে
  JSON-LD লিখতে হয়নি)। `noindex: true` দিলে `robots` নো-ইনডেক্স এবং JSON-LD বন্ধ।
- `escMeta()` দিয়ে মেটা ভ্যালু এস্কেপ, `jsonLdBlock()`-এ `</script>` ইনজেকশন আটকানো (`<` → `\u003c`)।

**অন্যান্য ঠিক করা ত্রুটি**
- **`/teachers` ছিল `/teacher-support`-এর হুবহু ডুপ্লিকেট** (একই পেজ, দুই URL — ডুপ্লিকেট কনটেন্ট)।
  সব অভ্যন্তরীণ লিংক `/teacher-support`-এ যায়, তাই `/teachers` এখন **৩০১ রিডাইরেক্ট**।
- **`/login` ও `/signup`-এ কোনো `<h1>`-ই ছিল না** — শুধু লোগো লিংক ও ট্যাগলাইন `<p>`। ট্যাগলাইনটিই `h1` করা হয়েছে।

**সচেতন সীমাবদ্ধতা**
- সাইটম্যাপে ১৭টি রুট — কারণ এটিই সব পাবলিক, স্বতন্ত্র URL-ওয়ালা পেজ। নোটিস/চাকরি/প্রশ্নপত্রের
  কোনো পার্মালিংক নেই (মডেলে খোলে), তাই ভুয়া URL বানিয়ে সাইটম্যাপ ফুলানো হয়নি।
- `SITE_ORIGIN = 'https://edusob.pages.dev'` — কাস্টম ডোমেইন যুক্ত করলে এক জায়গায় বদলাতে হবে
  (`src/routes/seo.ts` + `src/pages/layout.ts`)।

**রিগ্রেশন:** `h1audit` · `scan-inline-js` (১৫০ ব্লক, ০ ভাঙা) · `sec-test` · `xss-test` ·
`upsell-test` · `onboarding-test` · `cmd-palette-test` · `lazytab-test` · `csp-test` ·
`ratelimit-test` · `seo-audit` · `a11y-sweep` · `audit-sweep` — **সব ALL PASS**, `audit-sweep` → `ISSUES: none`।
`npm run build` → ১,৬৯৬.২৮ kB (gzip ৩৫৭.০১ kB)।

## ১৫. ডিপ্লয় ও নিরাপত্তা (সম্পন্ন — ২০২৬-০৯-১১)

**প্রোডাকশন: https://edusob.pages.dev** — রাউন্ড ৫ থেকে ১১ পর্যন্ত সবকিছু লাইভ।

যাচাই (`/api/onboarding` আগে ৪০৪ দিত = রাউন্ড ৪-এর বিল্ড; এখন ৪০১ = এন্ডপয়েন্ট আছে, অথেনটিকেশন লাগবে):

| চেক | ফল |
|---|---|
| `/api/onboarding` | ৪০৪ → **৪০১** ✅ (রাউন্ড ৫+ লাইভ) |
| `/robots.txt`, `/sitemap.xml` | ২০০ ✅ |
| `canonical` + `og:url` + JSON-LD | প্রতি পেজে ✅ |
| কমপ্রেশন | হোম HTML ১৪৯.৬ KB → **৩০ KB** ✅ |

**অ্যাডমিন পাসওয়ার্ড ঘোরানো (P0)**
- সমস্যা: পাসওয়ার্ডটি `migrations/0010_admin_account.sql`-এ প্লেইনটেক্সট-এ ছিল, আর
  `ON CONFLICT DO UPDATE SET password_hash = excluded.password_hash` থাকায় **মাইগ্রেশন চললেই
  পাসওয়ার্ড আবার হার্ডকোডেড মানে ফিরে যেত**। রিপোটি পাবলিক → কেউই পড়তে পারত।
- সমাধান:
  1. `migrations/0010_admin_account.sql` — প্লেইনটেক্সট মুছে দেওয়া, `DO UPDATE`-এ শুধু `role` রাখা
     (পাসওয়ার্ড আর কখনোই রিসেট হয় না)।
  2. `src/lib/db.ts` — লোকাল সিড ও D1 অটো-মাইগ্রেশন দুটো জায়গা থেকে প্লেইনটেক্সট সরিয়ে
     শুধু PBKDF2 হ্যাশ রাখা (কেবল ফ্রেশ ডেটাবেজে বুটস্ট্র্যাপ হিসেবে)।
  3. প্রোডাকশন D1 (`edusob-production`) — `UPDATE users SET password_hash=... WHERE phone='01829486022'`।
- যাচাই: নতুন পাসওয়ার্ডে লাইভ লগইন সফল, পুরনো পাসওয়ার্ড → `ভুল পাসওয়ার্ড` ✅
- ⚠️ নতুন পাসওয়ার্ড **রিপোজিটরিতে নেই** (শুধু PBKDF2 হ্যাশ) — পাসওয়ার্ড ম্যানেজারে রাখুন।
- ⚠️ পুরনো পাসওয়ার্ডটি গিট হিস্ট্রিতে থেকে যাবে (আগের কমিটগুলোতে)। হিস্ট্রি রিরাইট করতে চাইলে:
  `git filter-repo --path migrations/0010_admin_account.sql --invert-paths` — তবে এটি
  পাবলিক রিপোর হিস্ট্রি বদলে দেবে ও ফোর্স-পুশ লাগবে। পাসওয়ার্ড ঘোরানোর পর ঝুঁকি প্রায় শূন্য।

**টেস্ট স্ক্রিপ্ট:** `process.env.EDUSOB_ADMIN_PASS` থেকে পাসওয়ার্ড নেয় (ডিফল্ট লোকাল ডেভ DB-এর জন্য)।
প্রোডাকশনের বিপরীতে চালাতে: `EDUSOB_ADMIN_PASS=... BASE=https://edusob.pages.dev node <script>.mjs`

**পুশ:** `git remote add origin https://github.com/myofficialwebsite/EduSob` → `main` ব্রাঞ্চে পুশ করা হয়েছে।

## ১৬. বারো রাউন্ড — পারফরম্যান্স (সম্পন্ন)

নতুন টুল: `perf-audit.mjs` (Playwright, মোবাইল ৩৯০×৮৪৪ + CPU ×৪ + Fast-3G নেটওয়ার্ক)।

**মূল সমস্যা: `/board-challenge`-এ CLS ০.১১৩** (Google-এর "Good" সীমা ০.১)।

অনুমান না করে **আলাদা করে প্রমাণ** করা হয়েছে — একবারে একটি ফন্ট ব্লক করে CLS মাপা:

| পরীক্ষা | CLS |
|---|---|
| বেসলাইন | ০.০৯৮ |
| **Font Awesome আইকন ফন্ট ব্লক** | **০.০০২** |
| Noto Serif (ডিসপ্লে) ফন্ট ব্লক | ০ |
| দুটোই ব্লক | ০ |

**কারণ:** `<i class="fas ...">` আইকনের কোনো নির্দিষ্ট প্রস্থ ছিল না, আর আইকন ফন্ট
`font-display: block` ব্যবহার করে। ফলে ~২ সেকেন্ডে ফন্ট লোড হওয়ার আগ পর্যন্ত আইকনের প্রস্থ
০ থাকত — এরপর হঠাৎ প্রস্থ পেয়ে পুরো সারি নড়ে যেত।

**সমাধান (`tailwind.input.css`):**
```css
:where(.fa, .fas, .far, .fab, .fa-solid, .fa-regular, .fa-brands) {
  width: 1em; text-align: center; flex-shrink: 0;
}
```
`:where()` ব্যবহার করায় স্পেসিফিসিটি **০** — তাই Tailwind-এর `w-4`/`w-5` ক্লাস থাকলে সেটিই
জিতে নেয়, আর না থাকলে ১em প্রস্থ বরাদ্দ হয়। ফলে আইকনের বক্স ফন্ট লোডের আগে-পরে একই থাকে।

**একটি সমাধান বাতিল করা হয়েছে (মাপ করেই):** আইকন ফন্ট `<link rel="preload">` যোগ করলে
CLS ঠিক হলেও **হোমপেজের LCP ২.২৩৫s → ২.৬s খারাপ** হয়ে গিয়েছিল — ১৪.৭ KB ফন্ট LCP ইমেজের
সাথে ব্যান্ডউইথ নিয়ে পাল্লা দিচ্ছিল। প্রিলোড সরিয়ে শুধু CSS ফিক্স রাখায় দুটোই ভালো হলো।

**ফল (১৭ রুটের গড়, মোবাইল + CPU ×৪ + Fast 3G):**

| | আগে | পরে |
|---|---|---|
| `/board-challenge` CLS | ০.১১৩ ❌ | **০.০০২** ✅ |
| হোম LCP | ২.২৩৫s | **২.১০০s** ✅ |
| গড় LCP | ১.৫১১s | **১.৪৯১s** ✅ |
| গড় ট্রান্সফার (gzip) | ১৬১ KB | ১৬১ KB |
| থ্রেশহোল্ড ছাড়ানো রুট | ১ | **০** ✅ |

**ভিজ্যুয়াল যাচাই (প্রোগ্রামেটিক):** ১৯ রুট × ২ ভিউপোর্ট — কোনো ০-প্রস্থ আইকন নেই,
কোথাও অনুভূমিক স্ক্রল তৈরি হয়নি, আইকনের প্রস্থ ১০–৩০px (স্বাভাবিক সীমায়)।

**মাপা বেসলাইন (প্রোডাকশন, Cloudflare কমপ্রেশন সহ):** হোম HTML ১৪৯.৬ KB → **৩০ KB**।

**রিগ্রেশন:** ১৪টি স্যুট ALL PASS · `a11y-sweep` সব শ্রেণিতে ০ · `audit-sweep` → `ISSUES: none`।

## ১৭. তেরো রাউন্ড — ফর্ম ও ইনপুট UX (সম্পন্ন)

**লক্ষ্য:** সবচেয়ে কনভার্শন-ক্রিটিক্যাল জায়গা — ফর্ম। মোবাইলে ১৬px-এর ছোট ফন্ট
থাকলে iOS Safari আপনাআপনি জুম করে ফেলে; `autocomplete` না থাকলে ব্রাউজার অটোফিল
দেয় না; ভুল `type` হলে ফোন নম্বরে অঙ্কের কীপ্যাড আসে না। নতুন অডিটর
`form-audit.mjs` (১৫ রুট, ৩৯০×৮৪৪) দিয়ে মাপা হয়েছে।

### ১৭.১ ফর্ম অডিট — ১৯৩ → ০ সমস্যা

| পরীক্ষা | আগে | পরে |
|---|---|---|
| ১৬px-এর ছোট ফন্ট (iOS অটো-জুম) | ১৩৮ | **০** |
| `autocomplete` অনুপস্থিত | ২৫ | **০** |
| ভুল `input type` | ৬ → ২ (লগইন ব্যতিক্রম) | **০** |
| `inputmode` অনুপস্থিত | ১ | **০** |
| লেবেলহীন ক্ষেত্র (`aria-label` নেই) | ২৩ | **০** |
| মোবাইলে অনুভূমিক ওভারফ্লো | — | **০** |
| **মোট (১৮৪টি ক্ষেত্র)** | **১৯৩** | **০** ✅ |

**প্রধান পরিবর্তন (১৬px):** `tailwind.input.css`-এ শুধু `<768px`-এ
`:where(input,select,textarea):not([type=checkbox])…{font-size:16px!important}`।
`:where()` + `!important` ব্যবহার করায় Tailwind-এর `text-xs`/`text-[11px]` থাকলেও
মোবাইলে ১৬px-ই জিতে নেয়; ডেস্কটপের ঘন লেআউট (অ্যাডমিন টেবিল, CV এডিটর) অক্ষত।
যাচাই: ডেস্কটপে ১২৮০px-এ সব ক্ষেত্র আগের সাইজেই, কোথাও ওভারফ্লো নেই।

**`autocomplete`/`type`:** লগইনে `autocomplete="username"` + `current-password`,
সাইন-আপে `name`/`tel`/`new-password`, চেকআউটে `tel`, প্রোফাইলে
`name`/`email`/`bday`/`address-level1..3`/`organization`, CV-তে `name`/`street-address`।
সাইন-আপের ফোন `type="tel" inputmode="numeric"` (মোবাইলে অঙ্কের কীপ্যাড)।

> **ইচ্ছাকৃত ব্যতিক্রম:** `/login`-এর ফোন ক্ষেত্রে `type="text"`-ই রাখা হয়েছে —
> কারণ এটি ফোন **অথবা** ইমেইল দুটোই নেয় (`autocomplete="username"` দেওয়া আছে)।

**অডিটর সংশোধন:** লগ-ইন থাকলে `/signup` ও `/login` → `/login`-এ রিডাইরেক্ট করে,
ফলে অডিট ভুল পেজ মাপছিল — এখন এই দুটি রুট লগ-আউট কুকিতেই পরীক্ষা করা হয়।

### ১৭.২ CLS — হোমপেজের বারবারের শিফটের আসল কারণ

ফর্ম-ফিক্সের পর মনে হয়েছিল CLS বেড়েছে (হোমে ০.২৫–০.৩৫)। এব্লেশনে প্রমাণিত:
**কারণ ফর্ম নয়** — রুল বন্ধ করলে CLS আরও খারাপ (গড় ০.০৫৫৮ বনাম ০.০৩১৪)।
আসল কারণগুলো একে একে খুঁজে বের করে ঠিক করা হয়েছে:

| # | কারণ (প্রমাণ) | ফিক্স |
|---|---|---|
| ১ | ডিসপ্লে ফন্টের সোয়াপে শিরোনামের প্রস্থ বদলে লাইন ভেঙে যায় | **মেট্রিক-ম্যাচড ফলব্যাক** (`size-adjust:96.96%`, `ascent-override:112.63%`, `descent-override:51.78%`) — Hind Siliguri ৬০০ (৭.৮ KB, আগেই প্রিলোড করা) কে Noto Serif Bengali ৭০০-এর মেট্রিকে স্কেল করা। fontTools দিয়ে বাংলা ব্লকের ৯২ গ্লিফ মেপে গণনা |
| ২ | হিরো শিরোনামে `leading-snug`(১.৩৭৫) < ফন্টের কনটেন্ট-এরিয়া(১.৫৯em) → লাইন-বক্স **ফন্ট-নির্ভর** (৪৬px↔৬৭px) | `.font-serif-bn`-এ `line-height:1.65` (সব ফন্টের কনটেন্ট-এরিয়ার চেয়ে বড়) |
| ৩ | মোবাইল ইউটিলিটি বারে `flex-wrap` — ফন্ট আসতে আইটেম আবার সাজিয়ে সারি ৭২px→৬০px | মোবাইলে শুধু হেল্পলাইন (`hidden sm:flex` ইমেইল/সময়), `whitespace-nowrap` |
| ৪ | নোটিস টিকারের লাইন-বক্স ১৭px→২৮px | ধারক `h-7` + ট্র্যাকে `leading-7` |
| ৫ | আইকন ফন্ট `font-display:block` — দেরিতে এলে আইকন হঠাৎ আসে | ১৭ KB আইকন সাবসেট **প্রিলোড** |
| ৬ | **টিকার (আসল অপরাধী):** প্রতিটি `<span class="mx-6">`-এর শুরুতে ইমোজি — ইমোজি ফন্টের লাইন-বক্সে আইটেমের উচ্চতা ২৯→৫২px, প্রস্থ ৪২৪→৪৯৬px → পরের আইটেমগুলো সরে যায় (প্রোডাকশনে ৫/৫ বার CLS ০.১৩) | আইটেমকে `inline-flex` + স্থির `height:2.5rem`, ধারক `h-10` → তিন অবস্থাতেই (ফন্ট ব্লক/নোটো ব্লক/সব লোড) আইটেম ৪০px = ধারক ৪০px |
| ৭ | `<strong>`/বোল্ড টেক্সটে Hind Siliguri **৭০০** প্রিলোড না থাকায় দেরিতে লোড হয়ে প্রস্থ বদলাতো (w ৪৭→৬৩) | `hind-siliguri-700-bengali.woff2` প্রিলোড (৭.৬ KB) |

> **পদ্ধতিগত শিক্ষা:** এই সমস্যাটি লোকালে ধরা পড়েনি (perf-audit ৩ বার ALL PASS), কিন্তু
> প্রোডাকশনে ৫/৫ বার দেখা গেল। কারণ — লোকাল সার্ভার ধীর (LCP ~২২৫০ms) হওয়ায় ফন্ট
> প্রথম পেইন্টের *আগেই* পৌঁছে যায়; Cloudflare CDN দ্রুত (LCP ~১১৩০ms) হওয়ায় পেইন্ট আগে
> হয় এবং ফন্ট পরে পৌঁছায়। **তাই CLS সবসময় প্রোডাকশনের মতো দ্রুত পরিবেশেই মাপতে হবে।**
> একই কারণে আগের কয়েকটি A/B (হোম LCP ২০৯৩→১৫৯৯ms) ভুল ছিল — সেগুলো রেসপন্স
> ইন্টারসেপ্ট করে মাপায় কম্প্রেশন বদলে গিয়েছিল।

**চূড়ান্ত প্রোডাকশন পরিমাপ (মোবাইল CPU×৪ + Fast 3G, DSF ২, ১০টি পাবলিক রুট):**

| রুট | CLS | LCP |
|---|---|---|
| `/` | ০.০০০৬ | ১১৫২ ms |
| `/results` | ০.০০০৭ | ৯৬০ ms |
| `/admission` | ০.০৪৩ | ৯৩২ ms |
| `/scholarships` | ০.০০২১ | ৯১৬ ms |
| `/teacher-support` | ০.০১০২ | ৯৪৪ ms |
| `/mcq` · `/qpapers` · `/notices` · `/subscription` | ০–০.০০০৩ | ৮৮৮–১৪৮০ ms |

হোমপেজ: CLS গড় **০.০০১৩** (৬ বার; আগে ৪–৫/৫ বার ০.১৩) · LCP গড় **১১৩৮ ms**।

**২ নম্বর ফিক্সে একটি ভিজ্যুয়াল রিগ্রেশন ধরা পড়েছিল**, যা যান্ত্রিক টেস্টে পাওয়া যায়নি:
Tailwind-এর `.sm\:text-5xl{line-height:1}` (tw.css-এ ১০৮১১২ অবস্থান) আমার নিয়মের
(৮০৯৭০) পরে থাকায় ডেস্কটপে লাইন-হাইট ১ হয়ে যায় — ৩ লাইনের হেডিং ২৭৫px → ১৭৩px
(অত্যন্ত গাদাগাদি)। **ক্লাস দ্বিগুণ** করে (`.font-serif-bn.font-serif-bn`, নির্দিষ্টতা
০,২,০) `!important` ছাড়াই সমাধান → এখন ২৮৫px (মূল ডিজাইনের চেয়ে মাত্র +১০px)।

**পরিমাপে বাতিল করা দুটি প্রস্তাব (প্রমাণ সহ):**
- **ডিসপ্লে ফন্ট প্রিলোড (৪৫ KB)** — CLS ঠিক করে, কিন্তু ১৭ রুটে গড় LCP **+৫৮০ms খারাপ**
  (১১২০ms → ১৭০০ms, ২ বার × ৬ রুট)। বাতিল।
- **`font-display: optional`** — CLS-এ কোনো উন্নতি নেই (০.০৪২৬ বনাম ০.০৪৩১), LCP একই। বাতিল।

**আইকন ফন্ট প্রিলোডের লেনদেন (ইন্টারসেপশন ছাড়া খাঁটি তুলনা, ৩ বার করে):**

| | হোম LCP | CLS > ০.১ |
|---|---|---|
| প্রিলোড সহ | ২২৫৯ ms | **০/৩** ✅ |
| প্রিলোড ছাড়া | ২১৬৫ ms | ২/৩ ❌ (০.১৩১, ০.১৩৬) |

~৯০ms LCP দিয়ে বারবারের CLS-স্পাইক কেনা হলো — লেনদেনটি সপক্ষে। (বারো রাউন্ডে
এটি বাতিল করেছিলাম; সেই পরিমাপটি সময়-দূষিত ছিল — এবার ৩+৩ বার খাঁটি তুলনায় ভিন্ন ফল।)

### ১৭.৩ ফলাফল

| মেট্রিক | রাউন্ড ১২ শেষে | রাউন্ড ১৩ শেষে |
|---|---|---|
| হোম CLS | ০.২৫–০.৩৫ (বারবার) | **০** (টানা ৩ বার ALL PASS) |
| ফর্ম/ইনপুট সমস্যা | ১৯৩ | **০** |
| থ্রেশহোল্ড ছাড়ানো রুট | ০ | **০** ✅ |
| গড় ট্রান্সফার (gzip) | ১৬১ KB | ১৬৬ KB (আইকন প্রিলোড +১৭ KB) |

**রিগ্রেশন (১৫টি স্যুট):** `form-audit` ✅ · `h1audit` ০ · `scan-inline-js` ১৫০/০ ·
`sec` `xss` `upsell` `onboarding` `cmd-palette` `lazytab` `csp` `ratelimit` `quota-ui` ✅ ·
`a11y-sweep` সব শ্রেণিতে ০ · `seo-audit` ✅ · `audit-sweep` → `ISSUES: none` · `perf-audit` ✅।

### ১৭.৪ পার্শ্ব-পরিবর্তন

- `package.json`-এ **`playwright` যোগ করা হয়েছে (devDependency)** — এর আগে এটি
  অস্থায়ীভাবে ইনস্টল করা ছিল, তাই স্যান্ডবক্স রিসেটে মুছে গিয়ে সব টেস্ট স্যুট অচল
  হয়ে পড়েছিল। এখন `npm install`-ই যথেষ্ট।

## ১৭.৫ রাউন্ড ১৪ — রূপান্তর পথ (দর্শক → গ্রাহক)

নতুন অডিটর **`funnel-audit.mjs`** (`node funnel-audit.mjs`) — ল্যান্ডিংয়ের দাম/CTA,
`/subscription` লগইন-ছাড়া প্রবেশ, সাইনআপের ঘর্ষণ, `/wallet` + `/shop` পেমেন্ট পৃষ্ঠার
বিশ্বাস-সংকেত, লগইন-ছাড়া ট্রায়াল — ৫ ধাপ।

| খুঁজে পাওয়া | অবস্থা |
|---|---|
| ল্যান্ডিংয়ে দাম/CTA দেখা যায় | ✅ (false positive ছিল — নিচে দেখুন) |
| `/subscription` লগইন ছাড়া খোলে | ✅ HTTP ২০০, দাম `৳৫০ / ৳৩০ / ৳১০০` |
| সাইনআপের ইংরেজি ভ্যালিডেশন বার্তা | ✅ ঠিক — বাংলা `setCustomValidity` |
| `/wallet`-এ টাকা পাঠানোর পর কী হবে তা অজানা | ✅ ঠিক — ৩-ধাপ ব্লক + TrxID সতর্কতা + হেল্পলাইন |
| `/shop` চেকআউটে COD বোঝানো হয়নি | ✅ ঠিক — "হাতে পাওয়ার পরই টাকা দিন" + হেল্পলাইন |
| **প্রাইভেসি/শর্তাবলী/রিফান্ড নীতিমালা পেজ নেই** | ⚠️ **ব্যবসায়িক সিদ্ধান্ত** — নিচে দেখুন |

### ১৭.৬ আমার নিজের অডিটরে ধরা পড়া ২টি ভুয়া ফলাফল

1. **"`/subscription`-এ দাম নেই"** — ভুল। `tk()` (`subsPages.ts:৬৫`) দাম বাংলা অঙ্কে
   দেখায় (`৳৫০`), আর JS-এর `\d` শুধু ASCII অঙ্ক ধরে। রেজেক্স `[০-৯0-9,]`-এ পাল্টানোয়
   আসল চিত্র বেরিয়েছে: দাম আছেই, লগইন করা ও না-করা দুই অবস্থাতেই।
2. **"সাইনআপে এরর-বার্তা আসে না"** — ভুল। ব্রাউজারের নিজস্ব `pattern` যাচাই
   JS সাবমিট-হ্যান্ডলারের **আগে** আটকে দেয়, তাই পরীক্ষাটি অ্যাপ নয় ব্রাউজারের ভাষা
   মাপছিল। এবার সার্ভার-সাইড এরর (একই নম্বরে দ্বিতীয় অ্যাকাউন্ট) দিয়ে মাপা হয় —
   বাংলা ও সহায়ক বার্তা আসছে।

### ১৭.৭ ✅ সমাধান — নীতিমালা পেজ (৩টি)

আগের ধাপে যে ঘাটতি ধরা পড়েছিল, তা সমাধান করা হয়েছে **কোড-ভিত্তিক নীতিমালা** দিয়ে:
যেহেতু রিফান্ড সিস্টেমে আগেই বাস্তবায়িত ছিল (`shop.ts:335-346` — বাতিল হলে স্টক ফেরত +
`wallet_transactions`-এ `type='refund'`), তাই রিফান্ড নীতিমালা লেখা **কাল্পনিক প্রতিশ্রুতি
হয়নি** — এটি সিস্টেমের বাস্তব আচরণের বর্ণনা।

| রুট | বিষয় |
|---|---|
| `/privacy` | গোপনীয়তা — কী তথ্য নিই, কেন, কীভাবে সুরক্ষিত রাখি |
| `/terms` | ব্যবহারের শর্তাবলী — অ্যাকাউন্ট, সাবস্ক্রিপশন, ফলাফল-দাবি |
| `/refund` | পেমেন্ট, রিফান্ড ও ডেলিভারি — **সবচেয়ে বেশি রূপান্তর-প্রভাবশালী** |

**প্রতিটি দাবির উৎস (যাচাইকৃত):**

| দাবি | উৎস |
|---|---|
| রিফান্ড → ওয়ালেট ব্যালেন্সে, বিকাশ/নগদে নয় | `shop.ts:341-343` (`UPDATE wallets SET balance=balance+?`) |
| বাতিল এডমিন-মাধ্যমেই (`requireAdmin`) | `shop.ts:325` |
| ওয়ালেট অর্ডার = confirmed, COD = pending | `shop.ts:142` |
| পাসওয়ার্ড hash+salt, plain-text নয় | `api.ts:91` + `admin.ts:303` |
| সেশন ৩০ দিন, HttpOnly/Secure | `lib/auth.ts:94` |
| কোনো তৃতীয়-পক্ষ ট্র্যাকার নেই | `grep gtag/hotjar/fb` → ফাঁকা |
| ওয়ালেট থেকে নগদ উত্তোলনের ব্যবস্থা নেই | `grep withdraw` → ফাঁকা (মেন্টর-পেআউট ভিন্ন) |

**যা যোগ করা হয়নি (ইচ্ছে করেই):** কোনো রিফান্ড-মেয়াদ, ডেলিভারি-সময় বা SLA — কোডে
নেই, তাই লিখলে মিথ্যা হতো। COD চার্জের নির্দিষ্ট টাকাও লেখা হয়নি (এডমিন-নির্ধারিত,
চেকআউটে দেখানো হয়)।

**লিংক:** ফুটারের "সহায়তা ও নীতি" কলাম + `/wallet` টপ-আপ ফর্ম ও `/shop` চেকআউটের
**CTA-এর ঠিক পাশে** — যে মুহূর্তে মানুষ "টাকা ফেরত পাব?" ভাবে।

### ১৭.৮ রাউন্ড ১৪ রিগ্রেশন

`form-audit` ✅ ০ · `h1audit` ০ · `scan-inline-js` **১৫০/০** (নতুন স্ক্রিপ্ট ঠিক আছে) ·
`sec` `xss` `upsell` `onboarding` `cmd-palette` `lazytab` `csp` `ratelimit` `quota-ui` ✅ ·
`seo-audit` ✅ · `audit-sweep` → `ISSUES: none` · `perf-audit` ✅
(১৭ রুট, গড় LCP **১৬৩৮ ms**, থ্রেশহোল্ড ছাড়ানো ০)।

**নতুন রুটে a11y:** কনট্রাস্ট ৩টি + ট্যাপ-টার্গেট ১টি নিজেই ধরে ঠিক করা হয়েছে
(amber-600→700, slate-400→500, ফুটার লিংক ১৬px→২৮px)। অবশিষ্ট `kbd ⌘K` সাইট-হেডারের,
সব রুটেই সমান।

## ১৮. পরের ধাপ (রোডম্যাপ)

- **জরুরি:** এই অধিবেশনে ব্যবহৃত GitHub PAT ও Cloudflare টোকেন দুটিই **বাতিল করুন**
  (চ্যাটে উন্মুক্ত হয়েছে)।
- **জরুরি (নতুন):** GitHub PAT **মেয়াদোত্তীর্ণ বা ত্রুটিপূর্ণ** — `Invalid username or token`
  দেখিয়ে push ব্যর্থ। নতুন PAT দিলে কোড GitHub-এ পুশ করা যাবে (ক্লাউডফ্লেয়ার টোকেন
  ঠিক আছে, তাই ডিপ্লয় চলছে)।
- **সম্পন্ন:** নীতিমালা পেজ (§১৭.৭) — ৩টি রুট, প্রোডাকশনে লাইভ।
- **পরামর্শ:** নীতিমালা একবার আইনি দৃষ্টিকোণ থেকে পড়ে দেখান — বিশেষ করে রিফান্ড
  শুধু ওয়ালেটে ফেরত যায় (নগদে নয়) সেই ধারাটি।
- **আপনার করতে হবে:** Cloudflare ↔ GitHub অটো-ডিপ্লয় পুনরায় জুড়ে দিন (Workers & Pages
  → edusob → Settings → Builds & deployments) — Cloudflare এখন রিপোর ব্রাঞ্চ দেখতে পাচ্ছে না
  (`Unable to find a branch`, কোড ৮০০০০৩২), তাই ডিপ্লয় ম্যানুয়ালি wrangler দিয়ে করা হচ্ছে।
- **ঐচ্ছিক:** Cloudflare WAF Rate Limiting rule (D1 কাউন্টারের সাথে যুক্ত করে আরও কড়া করতে পারেন)

## ১৯. লোকাল প্রিভিউ
