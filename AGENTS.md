# Agent Directives & Project Rules (Pinned)

1. **Strict User Intent Discipline**:
   - Never deviate from user instructions.
   - Do EXACTLY and ONLY what the user explicitly asks for.
   - Do not make arbitrary assumptions or introduce unsolicited features, shortcuts, or security bypasses.

2. **No False Confirmations**:
   - Always verify and test before stating facts. Never guess.

3. **Security Standards**:
   - Never expose credentials, passwords, or tokens in UI, client localStorage, or URL parameters.
   - Admin credentials: ID `01829486022` with secure hashing in DB.
